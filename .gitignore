# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
.venv/

# Tooling caches (auto-regenerated; never commit)
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets
.env
polybot/config/.env

# Database WAL files (temp)
*.db-shm
*.db-wal

# Logs
polybot.log
polybot.log.*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
polybot/memory/recordings/
polybot/db/wallet_tape.db*
polybot/db/window_paths.db*
# Side-by-side live sidecar's isolated memory tree (POLYBOT_MEMORY_DIR) — a local
# dry-run artifact kept out of the nightly commit. The live DB (polybot_live.db)
# still commits with the paper DB as the trade audit trail.
polybot/memory_live/
