# Verdict — 08-31 strategy charter

**Verdict: (a) run unchanged.** Hold the deployed config (need 1.0, k[6,25],
$60 ladder, taker dormant) to the pre-registered floor re-decision on
**2026-09-11** (≥28 real-final days; proposal_floor_redecision_2026-09-11.md),
with a kill line now written down: projected §2-bar completion > 120 days at
trailing rates, or trailing-7-day deep-sell value < ~$450/day → escalate as a
kill-market decision, not another knob.

## Ranking (expected $/day at $400 × P(real) ÷ days-to-validation)

| candidate | E[$/day] | P(real) | days-to-val | score | disposition |
|---|---|---|---|---|---|
| run unchanged (1.0) | 5.6 era / ~3 now | ~0.8 (sign 0 flips/4,047; 5/5 fills won incl. 08-31 live; rung economics undecidable) | ~90 era-rate | 0.05 | **hold** |
| 0.75 floor at 09-11 | 11.2 era | ~0.8 × P(bar) ~0.5 | 11 + ~45 | 0.08 | survivor — pre-registered, waits for its date |
| lock-dip taker re-arm | — | — | — | 0 | precondition FAILS: 29/38 "reachable" dips are outage-frozen books (RESEARCH #3); real supply ~1 window / 1–2 wk |
| k_place [2,25] | +0 post-08-13 | — | — | 0 | 1 first-hour fill in 18 d; k≥6 scar stands |
| need 1.25 | 4 fills, $ = sweep lottery | — | — | 0 | flat; no |
| btc-15m ladder | ≤ pennies | — | — | 0 | REFUTED (r10; $137/day total pie ex-outlier) |
| rebate-financed quoting | $0 our pattern | — | — | 0 | three independent zero clauses; program expired 08-31 |
| post-close variants | ≤ ~$30/day visible pie | — | — | 0 | no N; 0/102 occupation refutation binds; collector-blocked |
| regime-offensive sizing | unmeasured | — | — | — | correlation test scheduled 09-11, measurement only |

Opportunity-cost line updated: the honest floor is **0.22–0.26 fills/day ≈
$5.6/day era-rate ($3/day at 08-28..30 supply), ~90-day bar clock** — not the
charter's ~$7/74d. Nothing inside the mandate beats it; 0.75 doubles it iff
its bar passes on schedule.

## The strongest case for the alternatives, and why not

**(b) modify to 0.75 now**: 2× accrual, 8/8 wins, 0 flip-fills, both OOS
halves positive; at 1.0 the bar needs a quarter-year. Rejected: the floor was
re-decided 08-27 with a ≥28-day reopening; moving it on 18 days of the same
corpus is threshold-lowering (the register's own ban), and 0.75's dollars are
two sweeps — the bar exists precisely to keep sweep lottery out of config.

**(c) kill the market**: supply −2.9× in three days, the lock structurally
excludes ~98% of the pie (r11: 0% of deep-sell volume arrives ≥1.0× p99.5),
venue volume −60% MoM, copycat pressure building, and the infra transfers
nowhere (btc-15m dead, siblings dead, taker dead) — kill means stop, and even
a perfect validation pays ~$6–11/day. Rejected for now: the realized epoch
evidence matches the model exactly (first fill 08-31, 0.26/day, +28¢/sh, the
0.80+0.65 rungs in full), running paper costs ~$0, the re-decision is 11 days
out, and the kill line above turns this judgment into a tripwire instead of a
mood. If the supply regime of 08-28..30 persists into mid-September, (c) is
the expected outcome of that tripwire — said plainly.

## Handed to the operator (proposals, not changes)

1. **Rule tripwire** (~20 lines, zero extra requests): compare Gamma
   `resolutionSource` + feeSchedule/tick/minSize at discovery against
   constants; mismatch → the existing `_on_source_mismatch` path. The 08-14
   silent change was visible in this field at the exact boundary.
2. **Post-expiry census** (09-01+): rewards ended 08-31; the walls and the
   135-sh at-price constant were measured in the subsidized regime.
3. Ops note: `postOnly` on GTC rungs guarantees maker-ness and is the only
   order type accepted in post-restart windows; venue had three placement
   outages 08-30/31 — live ladders must price cancel-only interludes
   (existing fail-closed cancel path suffices).

Deliverables: venue_truth_2026-08-31.md · ceiling_2026-08-31.md ·
proposal_floor_redecision_2026-09-11.md · this memo. Registers updated
(REFUTATIONS +btc-15m; RESEARCH #3/#6/#9/#11 + 08-31 section; WALLETS 08-31
census; CLAUDE.md §1/§5). Scripts r6–r11 + data in scripts/research/.
Config untouched; nothing committed.
