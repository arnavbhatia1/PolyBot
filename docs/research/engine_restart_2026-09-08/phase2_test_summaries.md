====================================================================================================
## None
REPORT: C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/premortem/REPORT.md
PREMISE_RISKS:
 - D4's log-loss table (information-structure/d4_logloss.csv) was computed without excluding the 253 dead-book windows -- re-check by diffing T1's re-verified numbers against this file to quantify how much of the reported log-loss gain the dead-book windows contributed.
 - The dead-book detector (book-dynamics/dead_book_windows.csv) was built only where a contested book coexists with |s|>=1 and can miss windows dead only while |s|<1 (book-dynamics' own stated gap) -- re-check by rebuilding an independent detector from window_paths book_age_*_s outliers and diffing the two exclusion sets.
 - The deployed-code re-arm claim (maker_bid.py:298-331 re-arms, ws2_ladder_replay.py:262-292 does not) rests entirely on reading code, never on an observed live/paper re-arm -- re-check the paper trade DB/logs for any actual multi-arm window before trusting any re-arm dollar figure in T2's verdict.
 - The contested-edge cells (+6 to +14pp, book-dynamics/contested_edge_clusterboot.csv) are measured at the book mid, not the executable ask, and not net of the ~0.4s taker RTT or the 0.07*p*(1-p) fee -- re-derive the same cells net of spread-crossing, RTT and fee before treating B1's pass as informative about B2's chances.
 - The maker ladder's fill-conditional win rates (book-dynamics/dumps_fill_conditional.csv, information-structure/d3_blind_winrate.csv) rest on a 'trough < rung' proxy indistinguishable in BBO data from a market-maker cancellation, with zero live ladder fills to calibrate it -- re-check against the handful of real recorded paper fills in polybot_paper_audit.db as a sanity cross-check.
 - The wallets scout's h1 pull (68 of 362 windows, selected for reversal-heavy wall capture) inverts the unconditional displacement edge inside the sample (loses 4-5pp vs the population's +1-2pp) -- re-check that no downstream T3 analysis silently pools h1 data into the fresh 09-02..09-07 comparison.
 - The 3,500-row pull cap drops the earliest rows in 324 of 355 windows, censoring exactly the long-horizon (120-240s, >240s) buckets where Phase-1 found its largest, most Bonferroni-significant wallet effects -- re-check by comparing the fresh (uncapped) pull's effect sizes in those same buckets against the old capped-corpus numbers to see whether the effect shrinks once censoring is removed.
 - Wallet class thresholds (top/bottom decile, >=70% one-directional, >=5 prior windows) were fixed 'a priori in-session but not pre-registered outside this session' per the wallets scout's own admission -- re-check whether any earlier version of s4_measure.py used different thresholds before settling on today's, indicating implicit tuning against visible partial results.
 - Book calibration slope >1 after 240s is attributed to the 0.99 tick cap rather than information, but the near-saturated P(win) surface at low r could interact with the tie->Up resolution convention -- re-check that T1's outcome label exactly reproduces window_labels.resolved_up's tie convention rather than a re-derived sign comparison.
 - Engine-map's exact reproduction of RESEARCH.md's 09-08 r27 number (17 fills / +$1,882.72) proves internal consistency between two invocations of the same fill rule, not that the fill rule itself is correct -- do not let T2 cite this reproduction as external validation of the fill model in any verdict.
====================================================================================================
## None
REPORT: C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/T4-whole-window-controls/REPORT.md
HEADLINE: P1 (book+z pre-zone model) and P2 (Binance UM bookDepth imbalance) both kill cleanly at every pre-registered k: the candidate feature is worse (higher log-loss), not better, than the recalibrated book at all 4+8 tested cells, in both chronological halves, robust to an independent z pipeline and to a flexible-recalibration sensitivity check. M'' (Chainlink-vs-Binance-relay basis) is UNDECIDABLE at all 4 P1 k-values because the recorded Binance relay stream never exists more than ~85s before close in the entire corpus (N=0). P3 is NOT_RUN: the 15m market meta.json files carry no strike/price_to_beat field in any of 282 files.

BARS:
 - [FAIL] P1-B1-Mprime-k240: measured=logloss_book=0.598763 vs logloss_M'=0.599041, mean_diff=+0.000155 (worse), boot p=0.6824, n_oos_days=17, N_eval=4217 | threshold=M' logloss < book_recal logloss, day-block p<0.05, >=15 OOS days | M' is worse than book, not marginally better; fails on direction before p-value is relevant. Confirmed in both chronological halves.
 - [FAIL] P1-B1-Mprime-k180: measured=logloss_book=0.494274 vs logloss_M'=0.494368, mean_diff=+0.000092, boot p=0.2860, n_oos_days=17, N=4197 | threshold=same as above | Worse in both halves.
 - [FAIL] P1-B1-Mprime-k120: measured=logloss_book=0.403066 vs logloss_M'=0.403138, mean_diff=+0.000076, boot p=0.1820, n_oos_days=17, N=3823 | threshold=same as above | Worse in both halves.
 - [FAIL] P1-B1-Mprime-k90: measured=logloss_book=0.371267 vs logloss_M'=0.371413, mean_diff=+0.000259, boot p=0.7608, n_oos_days=17, N=3275 | threshold=same as above | Marginally better in half1 (08-16..08-23) but worse in half2 (08-24..09-01); net worse, p=0.76 -> noise.
 - [UNDECIDABLE] P1-B1-Mdoubleprime-k240: measured=N=0 windows with a bz report at/before decision time | threshold=M'' (basis feature) beats recal book, same B1 bar | Binance relay (bz) stream never starts earlier than 84.99s before close in any of 6811 era windows with bz data; k=240 needs a report <= -240s. Data does not exist as specified, not a negative finding.
 - [UNDECIDABLE] P1-B1-Mdoubleprime-k180: measured=N=0 | threshold=same | Same data-coverage reason as k240.
 - [UNDECIDABLE] P1-B1-Mdoubleprime-k120: measured=N=0 | threshold=same | Same.
 - [UNDECIDABLE] P1-B1-Mdoubleprime-k90: measured=N=0 (earliest bz report ever recorded is -84.99s from close; k=90 needs <=-90s) | threshold=same | Closest of the 4 to the ~85s recording horizon; still zero coverage.
 - [FAIL] P2-Bonf-imb02-k240: measured=logloss_book=0.598760 vs logloss_M1=0.599030, mean_diff=+0.000297, raw p=0.0156, n_oos_days=17, N=4354 | threshold=M1(f) logloss<book, raw p vs Bonferroni alpha=0.05/8=0.00625, >=15 OOS days | Worse direction; fails both raw and Bonferroni-corrected bars.
 - [FAIL] P2-Bonf-imb1-k240: measured=mean_diff=+0.000635, raw p=0.0064, n_oos_days=17, N=4354 | threshold=same | Small raw p reflects consistently WORSE performance across days, not an edge; both chronological halves confirm worse.
 - [FAIL] P2-Bonf-imb02-k180: measured=mean_diff=+0.000304, raw p=0.1780, n_oos_days=17, N=4333 | threshold=same | 
 - [FAIL] P2-Bonf-imb1-k180: measured=mean_diff=+0.000526, raw p=0.0228, n_oos_days=17, N=4333 | threshold=same | Worse direction despite low raw p.
 - [FAIL] P2-Bonf-imb02-k120: measured=mean_diff=+0.000743, raw p=0.1504, n_oos_days=17, N=3953 | threshold=same | 
 - [FAIL] P2-Bonf-imb1-k120: measured=mean_diff=+0.000463, raw p=0.2032, n_oos_days=17, N=3953 | threshold=same | 
 - [FAIL] P2-Bonf-imb02-k60: measured=mean_diff=+0.000134, raw p=0.6580, n_oos_days=17, N=2378 | threshold=same | 
 - [FAIL] P2-Bonf-imb1-k60: measured=mean_diff=+0.000534, raw p=0.3596, n_oos_days=17, N=2378 | threshold=same | 
 - [NOT_RUN] P3: measured=0/282 .meta.json files under r10_pm_15m contain a strike/price_to_beat/question field; key set is identical across all 282 files (conditionId, outcomePrices, outcomes, clobTokenIds, volume, closed, orderPriceMinTickSize, orderMinSize) | threshold=derive strike_15m from meta; if not recoverable, NOT_RUN with reason | Pre-reg's own fallback clause invoked exactly; no external Gamma fetch attempted (out of this slice's authorization for P3).
KEY FACTS:
 - book_p built from window_paths.db (1Hz), nearest row within 0.6s of decision time, requiring two-sided book, mid_up+mid_down in [0.98,1.02], AND book_age_up_s/book_age_down_s <= 10s (deployed freshness gate) -- not merely last-BBO-change.
 - window_paths.db caps P1/P2 at ET days 08-13(partial)..09-01 (max ts = 2026-09-01 18:42 UTC), giving 20 unique ET days, 17 OOS after the first-3-train-only rule -- comfortably clears the >=15-day bar at every cell tested.
 - z (P1) reused directly from information-structure/d2_outside.py's outside_samples.parquet: z=(spot-strike)/rv60, rv60 from raw Chainlink receipt-clock reports, >=20 reports required in trailing 60s.
 - corr(logit(book_p), z) = 0.70-0.82 across k, and z's naive directional accuracy tracks just under book's naive accuracy at every k -- explains why M' can't beat the book: the book already prices this information (Phase 1 finding #1 reconfirmed walk-forward).
 - Binance relay (bz) stream: across all 6811 era windows with bz data, the earliest report is never more than 84.99s before window close (verified exhaustively) -- M'' is structurally unmeasurable at any P1 k (90/120/180/240), independent of whether the feature would help.
 - Binance UM bookDepth 'timestamp' column is a UTC datetime string 'YYYY-MM-DD HH:MM:SS' (neither ms nor us); alignment proven with an exact round-trip example (epoch 1786668601.0 -> '2026-08-14 00:50:01 UTC' -> 12 original rows at percentages -5..+5 matching exactly).
 - P2's pre-registered corrected test set is exactly 8 (imb02, imb1 x 4 horizons), matching the frozen doc's stated Bonferroni denominator 0.05/8; the 5-minute-change deltas were computed and scored but kept out of the corrected denominator per pre-mortem #15, and show the identical null pattern (7/8 worse).
 - Downloaded 19 daily Binance UM bookDepth zips (2026-08-14..09-01), 13.0MB total, 656,640 raw rows, 54,720 unique 30s timestamps -- matches the pre-registered ~13.8MB estimate.
 - Independent dead-book cross-check (pre-mortem #2): a refined placeholder detector (frozen near 0.50/0.51, single micro-tape record) built from book-dynamics/micro_b_dedup.parquet overlaps 216/253 (85%) of the existing dead_book_windows.csv; the 29 additional candidates it finds were re-run against the k=240 P1 dataset and changed zero rows (already excluded by the independent book_ok freshness/two-sided gate) -- no effect on the verdict.
 - Isotonic-recalibration sensitivity check (pre-mortem #14): isotonic recal of book_p is itself WORSE than the pre-registered 1-parameter logistic recal at every P1 k (e.g. k=240: 0.609374 vs 0.598763, p=0.0028) -- a naive 'M' beats isotonic-recal book' comparison would spuriously pass at all 4 k and is explicitly flagged as invalid, not credited.
 - Two independent z pipelines (raw-tick D2 cache vs a window_paths.chainlink_price-derived 1Hz proxy) give the identical qualitative kill at all 4 k (pre-mortem #13) -- the P1 result does not depend on which z computation is used.
PREMORTEM CHECKS DONE:
 - #1 rx-clock only: confirmed by construction and grep across all new scripts (build_bookp.py, build_bz.py, build_wp_zproxy.py, build_p2_features.py, model_p1.py, model_p2.py) -- zero uses of payload_ts/ets for any k or decision-time computation.
 - #2 Independent dead-book detector built from micro_b_dedup.parquet and diffed against dead_book_windows.csv: 85% overlap (216/253); 29 extra candidates found had zero effect on the P1 k=240 result when excluded (sensitivity re-run, identical N and logloss to 6 decimals).
 - #3 Every N is a distinct-(ep,k) row count (bookp.parquet dedups to one row per window per k); no dollar totals in this slice; no double counting.
 - #4 Walk-forward leakage check: tr=(et_day<d) strict, first 3 sorted days train-only in every script; verified string-sort of et_day exactly matches epoch-chronological order for this corpus (no wraparound).
 - #5 Day-block bootstrap B=5000 (>=2000), resamples whole ET days; n_oos_days (17 everywhere) reported next to every p-value.
 - #6 ANTI/edge<0: explicitly addressed as N/A for this test shape (symmetric log-loss model comparison, no directional bet, matching T1's own B1 precedent which also has no ANTI arm); substitute checks reported instead (coefficient-sign, days-improved near 50%, both-halves).
 - #7 Pre-declared-cell-first: P1/P2 grids computed and reported in the frozen doc's literal k/feature order; no cherry-picked cell quoted ahead of the full grid.
 - #8 Every applied bar diffed against the doc's literal wording (B1 bar text, Bonferroni 0.05/8) -- no threshold lowered or substituted anywhere.
 - #9 ws2_ladder_replay.py copy-before-edit: N/A, not used in this slice (no p99.5 table needed).
 - #10 Grepped this session's own commands/scripts for subscribe/websocket/place_order/polybot.main/run_polybot: zero hits; only network activity was pre-authorized public GETs to data.binance.vision.
 - #11 book_p required two-sided AND book_age<=10s (deployed freshness gate), enforced explicitly in build_bookp.py, not merely last-BBO-change.
 - #12 p99.5/margin-table byte-identity: N/A, explicitly noted -- neither P1 (uses rv60, not p99.5) nor P2 touches TWAP_MARGIN_P995 at all.
 - #13 z recomputed via two independent pipelines (D2 raw-tick cache vs window_paths-derived 1Hz proxy): verdict does not flip, both kill at every k.
 - #14 Isotonic-regression sensitivity check on the recalibrated-book baseline: isotonic is worse than logistic recal at every k; the resulting spurious 'M' beats isotonic' comparison is explicitly flagged invalid, not credited toward any pass.
 - #15 Confirmed P2's scored/corrected set is exactly 8 (2 features x 4 horizons); delta features scored separately in p2_context_results.csv with no Bonferroni column, kept out of the denominator.
 - #16 P2 restricted to Binance-UM-bookDepth-covered dates (08-14..09-01) joined against window_paths.db's own 09-01 cap; confirmed 17 OOS days at every one of the 8 primary cells, well above the >=15 bar -- P2 is a clean FAIL, not UNDECIDABLE-by-coverage.
 - #17 Cross-check meta strikes vs independent TWAP reconstruction: N/A, P3 is NOT_RUN so no meta-derived strike was used for anything.
 - #18 Explicitly scoped the P1/P2 kill to the narrow 2-3-feature logistic model class tested here, distinct from and not extending the wider 09-01 135-feature/GBT-class program closure referenced in MEMORY.md.
GAPS:
 - P1/P2 are hard-capped to ET days 08-13(partial)..09-01 by window_paths.db's own coverage ceiling, not by this slice's design -- 09-02..09-07 era days exist in win_streams.jsonl.gz but have no 1Hz book snapshot available, so cannot be added to P1/P2 without a fresh (out-of-scope) data collection.
 - M'' is UNDECIDABLE (not killed) at all 4 required P1 k -- a verified data-non-existence fact (the bz stream is never recorded more than ~85s before close in the whole corpus), so the candidate-F basis feature's actual usefulness at k in {90,120,180,240} remains genuinely untested; testing it would require either an infra change (record bz further back) or restricting to k<=85s, which falls inside T1's zone rather than P1's pre-zone.
 - P3 NOT_RUN: no price_to_beat/strike/question field exists in any of the 282 r10 15m .meta.json files; a live Gamma fetch was not pre-authorized for P3 in this slice and was not attempted, so the nested-strike dominance question remains fully open pending either a different data source or an explicit scope expansion.
 - The dead-book cross-check (pre-mortem #2) found the CSV's own detection method likely also catches a placeholder-oscillation pattern (many distinct dedup values hovering near 0.10/0.90) that a simple 'frozen near 0.5, <=1 record' proxy cannot see (37 CSV entries not matched) -- this residual method-difference was not further chased down since it had zero measured effect on the P1 verdict, but it means the 253-row CSV and any independent proxy of it are not perfectly interchangeable in general.
RECOMMENDATION: Adopt the T4 kill exactly as pre-registered: close the whole-window question (k>60s) to evidence for this narrow book+z / book+Binance-depth-imbalance logistic model class -- neither P1 nor P2 finds anything the book doesn't already know, and the P1 result reconfirms Phase 1's own finding that the open already prices the spot-vs-strike lag. Do not additionally cite this as extending the wider 135-feature/GBT program closure (different model class); if the operator wants that broader closure reaffirmed at k>60s specifically, it needs its own test. Treat M'' as a distinct, still-open data-infrastructure question rather than a killed hypothesis: if the operator ever wants the Chainlink-vs-Binance-relay basis tested at k>85s, the fix is recording the bz stream further back from close (an infra/logging change), not a different model. P3 stays open pending either a live Gamma pull for the 15m strikes (would need explicit re-authorization) or accepting the nested-strike question as untestable from local artifacts. No repo files were modified; all outputs are in the T4-whole-window-controls scratch directory.
====================================================================================================
## None
REPORT: C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/T1-zone-engine/REPORT.md
HEADLINE: B1 FAILS as written (passes only at k=45/30; fails at 58/20/10) and B2 FAILS its pre-declared cell (bars ii and v), so the kill clause applies: the projection's zone information is "real but not tradable as a taker" and the lock-gated ladder stays the only zone instrument. The load-bearing finding corrects Phase-1 D4: with the 253 dead-book windows excluded, M's log-loss gain over the recalibrated book collapses from 0.0135-0.0259 to at most 0.0018 (k 45/30), re-including them reproduces D4 to the third decimal, the projection is right in only 35-42% of book-vs-projection side disagreements (D4: 62-90%), and no advantage exists inside contested books at any k. The taker cell realizes +3.08 c/sh on 326 trades / 23 days (+$355.6, ANTI -11.2 c, control -0.65 c) but fails monotonicity (>=10 c bucket +4.9 < [6,10) +6.2, N 19/32) and significance (day-block p 0.072), and the aggregate rests on 26 book-leans-against trades and on price-improved fills that are themselves negative.

BARS:
 - [FAIL] B1 k=58: measured=bucket M vs logistic-recal book: LL 0.16381 vs 0.16453, diff -0.00072 (-0.44%), two-sided day-block p 0.193 (one-sided 0.097), 15/23 days better, N 5,939 windows, 23 OOS days | threshold=M OOS log-loss < recal book, day-block p < 0.05, >= 15 OOS days | D4 had -0.0135 p 0.0016; with dead-book exclusion the effect is 19x smaller and not marginal. Exact-k class (PM23) would pass (-0.00098, p 0.023); bridged r would pass (p 0.028); post-08-27 subset p 0.087; contested subset +0.0016 p 0.42 (wrong sign); vs isotonic recal +0.00006 p 0.99. k=59 context: -0.0004 p 0.48.
 - [PASS] B1 k=45: measured=LL 0.09921 vs 0.10098, diff -0.00177 (-1.75%), p 0.0002, 18/23 days, N 5,907, 23 OOS days | threshold=M OOS log-loss < recal book, day-block p < 0.05, >= 15 OOS days | Survives post-08-27 (p 0.0076, 11 days) and both halves (p 0.0012/0.0016); but the gain lives entirely in book-confident windows (-8.4% rel, p 0.0002) and is absent in contested books (-0.0013, p 0.49); vs isotonic recal not significant (p 0.29); vs spline recal p 0.0004. Ceiling 0.002 log-loss = sub-cent on a 0.99 book.
 - [PASS] B1 k=30: measured=LL 0.05417 vs 0.05599, diff -0.00182 (-3.25%), p 0.0076, 17/23 days, N 5,889, 23 OOS days | threshold=M OOS log-loss < recal book, day-block p < 0.05, >= 15 OOS days | Post-08-27 p 0.0004; H1 p 0.85 (not significant in the first half), H2 p 0.0002; contested subset -0.00095 p 0.88; vs isotonic recal p 0.65; vs spline p 0.0024. Same tail-sharpening-only character as k=45.
 - [FAIL] B1 k=20: measured=LL 0.02751 vs 0.02747, diff +0.00005 (+0.17%), p 0.861, 16/23 days, N 5,845, 23 OOS days | threshold=M OOS log-loss < recal book, day-block p < 0.05, >= 15 OOS days | Sign reversed vs D4 (-0.0259). Exact-k class also fails (p 0.35). No variant (noLive, age10, deadAny, bridged) passes.
 - [FAIL] B1 k=10: measured=LL 0.00664 vs 0.00643, diff +0.00021 (+3.3%), p 0.920, 13/23 days, N 5,828, 23 OOS days | threshold=M OOS log-loss < recal book, day-block p < 0.05, >= 15 OOS days | Sign reversed vs D4 (-0.0247); post-08-27 subset +0.0011 p 0.054 with the wrong sign. Exact-k class also fails (p 0.86).
 - [PASS] B2(i): measured=EW +3.08 c/sh, 326 filled trades (628 fires, 52% fill), 23 OOS days, win 73.9% (Wilson 68.9-78.4%); pre-declared cell e_min 0.05, L 0.42 s, price = ask(t+L) | threshold=EW >= +2 c/sh on >= 100 OOS trades over >= 15 OOS days | Fragile: priced at ask(t) the same trades read -0.57 c/sh; price-improved fills (N 190) -1.46 c vs unchanged-ask fills (N 136) +9.41 c; contested sub-population (N 278) +1.42 c p 0.26; 26 book-leans-against trades carry +$156 of the +$356.
 - [FAIL] B2(ii): measured=sweep partition EW by bucket {<0: -0.65 (N 5,543), [0,3): +0.00 (4,700), [3,6): +2.18 (121), [6,10): +6.25 (32), >=10: +4.90 (19)}; own-arm buckets of the 0.05 trades [5,6) +1.48 (121), [6,10) +1.38 (146), >=10 +10.56 (59) | threshold=net c/sh non-decreasing across {<0,[0,3),[3,6),[6,10),>=10} | Top bucket below [6,10) in the sweep partition (N 19); own-arm also non-monotone ([6,10) < [3,6)). The anti-predictive signature (control = best cell) is absent - the control is the worst bucket in both partitions. Fails in every context cell too (L 0.70 sweep: >=10 bucket -0.65, N 18).
 - [PASS] B2(iii): measured=<0 control (first tick with edge < 0): EW -0.65 c/sh, N 5,543 fills / 5,931 fires, -$784, mean price 0.964; mirror control (edge <= -5 c): -5.95 c/sh, N 272 | threshold=<0 control bucket EW <= 0 | Control is dominated by buying the 0.99 favorite at k~58 when the model says it is slightly overpriced.
 - [PASS] B2(iv): measured=ANTI (other side's token at the same fire tick, same FOK rule): EW -11.25 c/sh, N 150 fills, -$712, other side won 19.3%, mean price 0.293 | threshold=ANTI EW <= 0 | ANTI negative in every context cell (-8.0 to -24.9 c).
 - [FAIL] B2(v): measured=day-block bootstrap p(EW <= 0) = 0.072 (two-sided 0.144), 95% CI [-1.00, +7.50] c/sh, 23 unique day blocks, B = 5,000 | threshold=day-block p(EW > 0) < 0.05 | 13 of 23 days positive; top-3 days carry 69% of dollars. Context cells e_min 0.08 (p 0.023) and L 0.70 (p 0.046) pass (v) but fail (ii); adoption reads only the pre-declared cell.
 - [PASS] B2(vi): measured=H1 (OOS days 08-16..08-25, N 196) +$156.6; H2 (08-26..09-07, N 130) +$199.0 at $15 notional | threshold=both halves > $0 | Depth-filtered subset (size-known 08-22..09-01, N 62) has H1 -$9.4; e_min 0.08 cell H1 +$64 vs H2 +$333.
KEY FACTS:
 - Dead-book exclusion is the whole D4 story: rebuilding D4's population (no dead exclusion, no liveness, bridged r, exact-k) gives diff -0.01377/-0.01828/-0.02132/-0.02593/-0.02737 at k 58/45/30/20/10 (D4: -0.0135/-0.0182/-0.0213/-0.0259/-0.0247); excluding the 253 CSV windows leaves at most -0.0018. The liveness filter alone does not remove them (frozen placeholder books still emit size-only updates).
 - With dead books excluded the projection is right in only 35/42/35/38/38% of book-vs-projection side disagreements at k 58/45/30/20/10 (N 178/96/55/32/21); with them included 63/74/77/85/91% (D4: 62-90%).
 - All surviving B1 gain is inside book-confident windows (P outside [0.05,0.95]): k 45 -8.4% rel p 0.0002 on 21/23 days; inside contested books M is never better than the recalibrated book (p >= 0.16 at every k; wrong sign at 58/59/20/10).
 - Against an isotonic recalibration of the book the k=45/30 advantage is not significant (p 0.29/0.65); against a 5-knot spline it is (p 0.0004/0.0024) - part of the claim is 'beats an under-fit logistic recal'.
 - Exact-k model class (D4 design) flips k=58 to pass (-0.00098, p 0.023, -0.6% relative) - the k>=25 kill decision is model-class dependent only at k=58 and sub-percent in either class; k=20/10 fail in both classes.
 - B2 pre-declared cell: 628 fires / 326 fills / 23 days, EW +3.08 c/sh, +$355.6 (H1 +$156.6 / H2 +$199.0), ANTI -11.25 c (N 150), control -0.65 c (N 5,543), p(EW<=0) 0.072, monotonicity fails at the >=10 c bucket (+4.90, N 19, below [6,10) +6.25, N 32).
 - Fill mechanics dominate the sign: 302 of 628 fires missed because the ask rose within 0.42 s (those would have won 84% vs 74% for fills); fills at an improved ask (N 190) run -1.46 c while unchanged-ask fills (N 136) run +9.41 c; pricing every fill at ask(t) gives -0.57 c/sh (p 0.61).
 - Sub-populations: contested (book P of side in [0.5,0.9], N 278) +1.42 c p 0.26; book leans against (P < 0.5, N 26, mean book P 0.47, price 0.45) +18.6 c p 0.011 carrying +$156; confident-for (N 22) +5.7 c. k>=25 lane (N 281) +2.26 c p 0.18; k<25 (N 45) +8.17 c.
 - P_M is over-confident by 10-14 pp on the bulk of fills (bins 0.7-0.95 realized 66-80%); the profit sits in the (0.5,0.7] bins (65-69% realized at prices 0.46-0.49).
 - Fires: 27.3/day (median 22), k p10/p50/p90 = 23.6/44.7/57.4 s, buckets [45,60] 290 / [25,45) 253 / [15,25) 56 / [6,15) 29; chosen-side book P at fire p5/p50/p95 = 0.495/0.765/0.905.
 - Ask size (window_paths, 08-22..09-01, 140 size-known fires): 14.3% of fires have top ask < 15 sh; 21.5% of size-known trades exceed top-of-book depth; depth-OK subset (N 62, 8 days) +8.75 c but H1 -$9.4, p 0.093.
 - Freshness gate (two-sided + raw record <= 10 s at t and t+L): 0 of 326 trades dropped.
 - Context grid: e_min 0.03 +0.29 c (N 489, p 0.46); e_min 0.08 +7.17 c (N 209, p 0.023, H1 +$64 / H2 +$333); L 0.70 at e_min 0.05 +3.58 c (N 294, p 0.046); (ii) fails in every cell.
 - Verification: ticks.parquet re-derived on 11,548 sample ticks with 0.0 diffs (rx clock, k = close - rx); TWAP_MARGIN_P995 == r1_tables()['P995'] asserted; labels = window_labels.resolved_up asserted for all 7,018 windows (100 from polybot_live.db as ws1_reduce.py does), 0 exact ties; walk-forward asserted per day (first 3 ET days train-only, 23 OOS days 08-16..09-07).
PREMORTEM CHECKS DONE:
 - PM1 clock discipline: all k use receipt rx; ticks.parquet verified against a fresh common.Window.proj (chainlink_feed.py replica) on 11,548 sample ticks - k diff 0.0, projection diffs 0.0, None-agreement 100%; grep of T1 scripts: no payload_ts/ets in any k computation. PASS.
 - PM2 independent dead-book detector: micro-based flags 356 windows (158 not in CSV; 134 of those have zero book records and drop out via undefined book_p, 32 are frozen-while-contested), window_paths both-sides-frozen flags 200 of 5,222 covered windows (52 not in CSV); CSV windows missed: 55 (micro) / 26 (paths). B1 re-run with the union excluded (P_deadAny) changes no verdict; reported, primary population kept as pre-registered.
 - PM3 once per window: B1 one row per window per k; B2 N trades == N windows (326) in every arm; tick-level rows used only for fitting. PASS.
 - PM4 walk-forward assert: t1lib asserts max(train et_day) < test day for every day; 23-day log in b1_models_P_plain.json (08-16 trains on <= 08-15 with 25,449 ticks; 09-07 on <= 09-06 with 315,593). No shuffling. PASS.
 - PM5 bootstrap: B = 5,000 whole-ET-day resamples everywhere; unique day counts next to every p (23 / 11 post-08-27 / 12 inside / 10 H1 / 13 H2). PASS.
 - PM6 ANTI + edge<0 control computed and reported for the pre-declared cell and all five context cells. PASS.
 - PM7 pre-declared first: b2_lock_predeclared.json stamped 2026-09-09T13:59:54Z; first context cell 13:59:55Z; verdict quotes the pre-declared cell first. PASS.
 - PM8 bar wording diff (REPORT section 5): no numeric bar deviates; interpretive choices (two-sided p for B1, price = ask(t+L) with ask(t) variant reported, e_min=0 sweep partition for (ii), halves boundary 08-25) listed, none loosens a bar.
 - PM9 repo read-only: git status shows only the pre-existing untracked pre-registration file; ws2_ladder_replay.py imported read-only for r1_tables(), never edited or copied. PASS.
 - PM10 no live wire: grep of all T1 scripts + cmdlog.txt for subscribe/websocket/ws:///wss:///place_order/cancel_order/polybot.main/run_polybot: zero hits. PASS.
 - PM11 book_p freshness: book_p always from a two-sided token (own or complement), primary liveness <= 3 s is stricter than age <= 10 s; the age-10 variant (P_age10) changes no verdict. PASS.
 - PM12 p99.5 table identity: TWAP_MARGIN_P995 == ws2_ladder_replay.r1_tables()['P995'] asserted True. PASS.
 - PM13 post-08-27 split: k 45/30 survive alone (p 0.0076 / 0.0004, 11 day blocks); k 58/20/10 fail on both subsets; no table-leakage claim needed.
 - PM14 flexible recal: vs isotonic the k 45/30 advantage is not significant (p 0.29 / 0.65); vs spline it is (p 0.0004 / 0.0024) - material shrinkage, part of the claim is 'beats an under-fit book'.
 - PM15 confident vs contested: the advantage exists only in book-confident windows; contested subset shows none at any k (p >= 0.16). The claim does not survive the contested subset.
 - PM16 k=58 for k=60 stated explicitly; k=59 added: diff -0.0004, p 0.48 (fail).
 - PM17 ask size: 14.3% of size-known fires < 15 sh; 21.5% of size-known trades exceed depth; depth-OK subset (N 62, 8 days) EW +8.75 c but fails (i) N/days, (v) p 0.093, (vi) H1 -$9.4.
 - PM18 freshness at t and t+L: 0 of 326 trades dropped; fresh-only EW equals the cell's.
 - PM19 label source: up = window_labels.resolved_up (paper DB + polybot_live.db for 100 08-14 windows), asserted equal to win_streams.up for all 7,018; strike/final asserted equal; 0 exact ties. PASS.
 - PM20 e_min 0.05 locked before 0.03/0.08/L 0.70 (timestamps); post-lock 3.2 splits are descriptive only, no bar adjusted. PASS.
 - PM21 OOS edge for buckets: P_M from the (day, bucket) model trained on days < day evaluated at the fire tick; no refit. PASS.
 - PM22 effect size at k>=25: k 58 -0.0007 (-0.44%), p 0.19, 15/23 days vs D4 -0.0135 - not a marginal under-powered miss (19x smaller; sign reversed at 20/10); extension to 09-08 impossible (no local 09-08 recordings); the dead-book diagnostic explains D4.
 - PM23 exact-k class: k 58 flips to pass (p 0.023, -0.6%); 45/30 pass in both classes; 20/10 fail in both; kill decision at k>=25 is class-dependent only at k=58.
 - PM24 scope statement in the verdict: nothing here licenses loosening the ladder's r >= 0.6 lock (T2's adverse-selection question).
GAPS:
 - window_paths ends 09-01 18:42 UTC, so ask sizes/book ages cover 8 of 23 OOS days (140 of 628 fires); the micro tape has no sizes - FOK fills assume the top-of-book ask has the $15 (21.5% of size-known trades exceed it).
 - No 09-08 recordings exist locally; the corpus ends 09-07 (23 OOS days) - the PM22 extension could not be run.
 - Fill price convention (ask(t+L), the matched resting price) is not specified in the pre-registration; the ask(t)-priced variant flips the cell to -0.57 c/sh and is reported, not adopted.
 - The (ii) bucket partition is implicit in the pre-registration; the e_min=0 sweep + <0 arm was used because it is the only partition populating all five buckets; both it and the own-arm reading fail.
 - Bucket-model training rows are autocorrelated ticks (affects only fits, not any reported N or p); isotonic predictions clipped to [1e-3, 1-1e-3].
 - Fee = deployed 0.07*p*(1-p) only; no maker rebate, slippage beyond the top level, or deployed book gates (depth >= $50, price-sum) modeled.
 - The 100 08-14 windows labeled only in polybot_live.db have no micro book records (Phase-1 prep_micro used paper-DB labels) and drop out via undefined book_p in both Phase 1 and here.
 - Tie -> Up rule never exercised (0 exact final == strike in the corpus).
RECOMMENDATION: Record T1 as killed per its own clause: B1 fails as written and B2 fails (ii) and (v) in the pre-declared cell, so no taker-side probabilistic engine is licensed at k in [25,60] and the lock-gated ladder remains the only zone instrument; this result says nothing about the ladder's r >= 0.6 lock, which is T2's question. Supersede the Phase-1 D4 entry in the pre-registration's map (item 2) with a note that its 0.0135-0.0259 log-loss gain and 62-90% disagreement accuracy were the 253 placeholder-book windows; the corrected picture is a sub-cent tail-sharpening at k=45/30 confined to already-decided books, invisible inside contested books and not significant against an isotonic recalibration. Do not pursue the e_min 0.08 / L 0.70 cells that pass (v): they were context, every cell fails monotonicity, and the taker aggregate is carried by 26 trades and by fills whose sign flips under the ask(t) pricing convention. If the operator wants any follow-up here, the only honest one is infrastructural, not statistical: the 48% FOK miss rate and the -1.5 c/sh on price-improved fills show the taker is adversely selected within 0.42 s, so the question is fill mechanics with real sizes (a live, tiny-notional latency/size probe), not another model.
====================================================================================================
## None
REPORT: C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/T2-ladder-hygiene/REPORT.md
HEADLINE: The re-arm state machine reproduces all three pre-registered loser windows (and the 09-07 live loss at k_max 58) as re-arm flip-fills, and every loser-side re-arm dollar in 26 days (4 windows, -$475 incl. a fourth loss ep 1787678700 the pre-registration did not name) sits in the first half while re-arm wins are single +$6.25 fills; but the pre-registered "one arm per window" rule fails by the letter because the second half has zero re-arm fill events (0 vs 0). The H2 reprice cancel fires before every known loss on trailing-only information yet is the same event that produces winner fills, forgoing $1,070 to remove $100 (FAIL); relaxed schedules S1/S2 lose dollars in the first half, add 4-6 losses and 5-7 relaxed-rung flip-fill losses under production's placement-time-only rung filter (FAIL). A Phase-1 artifact defect surfaced: dead_book_windows.csv contains 28 live-book windows including 2 of the 3 loser windows and 7 of the 17 baseline fill windows (+$793.66); T2's verdicts are invariant to it, other slices may not be.

BARS:
 - [PASS] H1-faithfulness: measured=re-arm ON, need 0.6, k_max 25, R8, $200 (no exclusion, N=6,358 armed): ep 1787271900 -$100.00 (arm 3/3), 1787358600 -$100.00 (arm 2/2), 1787620800 -$75.00 (arm 3/3) = 3/3 flip-fill losses; ep 1788743700 at k_max 58: -$75.00 on arm 1 (k 49.7, 119.7 sh), identical to today. Hold-out 1788267900: NOT a loss (bridged r toward Up peaks 0.58 in [6,25]; armed Down k 17.2 = winner; book-dynamics used the plain projection). | threshold=>= 2 of 3 known loser windows reproduce as flip-fills; 09-07 live loss reproduces at k_max 58 | Regression with the unmodified repo harness reproduced RESEARCH.md exactly (17/17/+$1,882.72 at k25; 28/26/+$1,956.28 at k58). run2(rearm=False) is row-identical to run() (6,358/6,358 and 6,394/6,394). 134 logged era windows with >=2 MAKER LADDER lines confirm production re-arms; harness cross-check reproduces 34 of 66 logged floor re-arms (harness under-counts: production evaluates a drifting projection at 4 Hz).
 - [FAIL] H1-adoption (one arm per window): measured=k25, protocol exclusion (N 6,107 armed, 26 ET days): H1-half loser $ removed 100.00 vs winner $ forgone 6.25 (2 re-arm fills); H2-half 0.00 vs 0.00 (0 re-arm fills). No exclusion: H1-half 475.00 vs 6.25 (5 re-arm fills: 4 losses -100/-100/-75/-200, 1 win +6.25); H2-half 0 vs 0. k58 context: 475.00/62.50 H1-half, 0/6.25 H2-half. Day-block bootstrap ON-OFF (26 days, B=5000): -93.75, 95% CI [-300.0, +18.75] (protocol). | threshold=loser $ removed > winner $ forgone in BOTH halves (strict) | Fails only because the second half (08-26..09-07) carries no re-arm fill at k_max 25 under any exclusion variant (0 > 0 is false); the first half passes 16:1 (protocol) / 76:1 (no exclusion). Direction of all evidence favors one-arm; the rule as written cannot be met without second-half events. Zero-delta inspection: 6,105 zero-delta windows = 6,041 single-arm + 64 multi-arm with no later fill + 0 with a later fill (no blind-replay pattern).
 - [FAIL] H2-adoption (reprice cancel): measured=k25, thr 0.05, re-arm ON + latch, protocol exclusion: base 12 fills +$995.31 -> H2 2 fills +$12.50; H1-half loser $ removed 100.00 vs winner $ forgone 1,070.31; H2-half 0.00 vs 12.50; reprice-cancel windows 39; ANTI -808,417 -> -805,367 (changed). One-arm regime: 0.00/1,064.06 and 0.00/12.50. Latency 0.25 s: 0 removed / 1,057.81 forgone (the loss still fills). thr 0.03/0.08: same as 0.05. Trailing-only trigger precedes the first fill in all 3 known losses + live loss (lead 0.09-1.10 s, complement ask +0.16..+0.54); trailing vs +-1 s peeking agree on all; no classification flips at 0.03/0.08. | threshold=loser $ removed >= 2 x winner $ forgone in both halves; ANTI unchanged | The reprice signal is real but is the sweep itself: every winner fill is our ask falling / the complement ask rising. Day-block bootstrap H2-base: -982.81, 95% CI [-3,142, +169]. Same verdict under confirmed-dead-only exclusion and at k58.
 - [FAIL] H3-S1: measured=k25, re-arm ON, protocol exclusion: S1 13 fills / 8 wins / 5 losses / +$797.03 vs S0 12/11/1/+$995.31; H1-half +769.35 vs +982.81 (fails), H2-half +27.68 vs +12.50; losses 5 vs 1; relaxed-rung flip-fill loss windows 5 (per window); ANTI -755,342 (<= 0). Confirmed-dead-only: +1,138.77 vs +1,413.97, losses 8 vs 4, 7 relaxed-rung loss windows. k58 ON: +904.31 vs +1,105.41, losses 9 vs 3, 9 relaxed-rung loss windows. OFF regime: all cells fail too. | threshold=dollars >= S0 in both halves, loss count <= S0, no flip-fill loss on any relaxed rung, ANTI <= 0 | Production semantics: rungs are filtered only at placement (no late joins), so S1 rests the 0.80/0.65/0.50 rungs in 4,649 of 6,301 armed windows vs 6,107/6,107 for S0 (e.g. 1786666200: +$988.10 vs +$1,032.81). The 0.35 rung at need 0.3 fills 7 windows and wins 2 against a 35% break-even.
 - [FAIL] H3-S2: measured=k25, re-arm ON, protocol exclusion: S2 16 fills / 9 wins / 7 losses / +$836.09 vs S0 +$995.31; H1-half +761.98 vs +982.81 (fails), H2-half +74.11 vs +12.50; losses 7 vs 1; relaxed-rung flip-fill loss windows 7; ANTI -724,828. k58 ON: +$1,582.43 vs +$1,105.41 total but H1-half +1,054.67 vs +1,154.44 (fails), losses 16 vs 3, 15 relaxed-rung loss windows; day-block bootstrap S2-S0 at k58: +477, 95% CI [-497, +1,590]. Confirmed-dead-only k25: +981.40 vs +1,413.97, losses 10 vs 4. | threshold=dollars >= S0 in both halves, loss count <= S0, no flip-fill loss on any relaxed rung, ANTI <= 0 | S2's extra dollars at k58 come from the 0.15 rung (12 fill windows, 6 wins, +$700) bought with 13 extra losses. AT_PRICE_QUEUE_SH=135 on the 0.15/0.10/0.05 rungs is an unverified extrapolation shared by S0.
 - [UNDECIDABLE] H4-descriptive: measured=protocol exclusion, re-arm ON: need 0.6 k15 5 fills/5 wins/+$1,057.81 (no re-arm fills; ON==OFF); need 0.6 k25 12/11/+$995.31 (OFF 10/10/+$1,089.06); need 0.8 k15 1/1/+$6.25, k25 2/2/+$12.50 (1 re-arm win); need 1.0 k15 1/1/+$6.25, k25 1/1/+$6.25. Confirmed-dead-only: need 1.0 k25 OFF 5 fills/5 wins/+$596.27 (full table in h4_results_confirmed_dead_only.json). ANTI <= 0 in every cell. | threshold=none (descriptive; adoption owned by the 09-11 re-decision) | Non-adoptive by pre-registration; labeled so in the report and kept out of every H1/H2/H3 total. The protocol exclusion strips most honest-floor fills (they sit in CSV-live windows), which is why the two exclusion variants differ so much here.
KEY FACTS:
 - Production after a floor cancel: _retire() always books (if fills>0 and notional>=$1) and always sets active=None; the placement hook refuses only when a position exists for the window (main.py:1048-1055) -> zero-fill cancel re-arms, partial fill blocks; maintain() precedes placement in the loop so a flip can re-arm on the same tick. Verified by unit tests and by 134 logged multi-arm era windows (119 floor/flip re-arms, 0 fills on arm>=2 in the 07-13..08-27 logs).
 - Regression: unmodified repo harness reproduces RESEARCH.md 09-08 exactly (17/17/+$1,882.72 at k25; 28/26/+$1,956.28 at k58, ep 1788743700 -$75.00); repo file untouched (md5 fbce52f8899c8595dc02fd576792518c); P995 table value-identical to TWAP_MARGIN_P995.
 - Faithfulness gate 3/3: all three pre-registered loser windows are re-arm fills (arms 3, 2, 3) and vanish under one-arm; a FOURTH re-arm flip-fill loss exists at k25 (ep 1787678700, 08-25, arm 2, -$200) hidden by the CSV exclusion; the 4th book-dynamics loss (1788267900) is not a bot-zone loss under the bridged sign (plain-vs-bridged artifact).
 - Re-arm economics, 26 ET days, k25, no exclusion: 5 re-arm fills = 4 losses (-$475) + 1 win (+$6.25); all in the first half (08-20..08-25); second half has no re-arm fill at k25 -> the pre-registered both-halves rule cannot pass. k58: 14 re-arm fills, -$475 vs +$62.50 (H1-half), 0 vs +$6.25 (H2-half).
 - H2 reprice trigger precedes the first fill in all 3 known losses and the live loss on trailing-only information (lead 0.09-1.10 s, complement ask +0.16..+0.54), robust to thr 0.03/0.05/0.08 and identical to the +-1 s peeking classification; but it removes 10 of 12 winner fills too (k25: $100 removed vs $1,070 forgone) and with 0.25 s decision latency no longer catches the loss.
 - H3: with production's placement-time-only rung filter, S1/S2 arm early with only relaxed rungs and drop the 0.80/0.65/0.50 rungs from 26% of armed windows; the 0.35 rung at need 0.3 wins 2 of 7 (S1) / 3 of 7 (S2) fill windows vs a 35% break-even; S1/S2 add 4-6 losses at k25 and 6-13 at k58.
 - dead_book_windows.csv (253) was built as 'contested book while |s|>=1', not from activity: 225 confirmed dead (<=2 BBO records, placeholder books), 28 live (226-2,122 BBO changes) incl. loser windows 1787271900/1787620800 and 7 of 17 baseline fill windows (+$793.66; 1787581200 has 2,122 changes and +$557.81). The pre-registered exclusion drops 42% of the baseline dollars; T2 verdicts are invariant to it.
 - certain_winner replica (pre-mortem 20): 702 of 6,107 armed windows would fail closed at close+5 s (open capture untrusted 408, close 330), 16 'wrong winner' cancels all pre-08-18 (retired 30 s topic in the recording); 0 post-close fills in any run, dollars unchanged.
 - Harness vs production logs (need 1.0 / old tables, 08-18..08-27): first-arm k agrees within +-1 s for 80% (median +0.45 s, N 2,094); harness reproduces 34 of 66 logged floor re-arms (8 not in logs) -> re-arm exposure here is a lower bound on production.
 - ANTI control (same rule, other side) loses ~$200 per armed window in every cell (the losing token prints through every rung); it is the edge<0 control for a binary-sign ladder (no continuous model edge exists to bucket).
PREMORTEM CHECKS DONE:
 - 1 rx clock: grep of harness+drivers - k, placement, cancel, fill windows all on receipt clocks; payload ts used only where production does (Binance bridge anchor, boundary trust ts-B<=0.5). No ets anywhere.
 - 2 independent dead-book detector: micro-tape <=1 BBO change on a token (642) and window_paths book age >60 s (224 of 5,260 through 09-01); vs CSV(253): CSV∩A 223, CSV\A 30; classified CSV: 225 confirmed dead, 28 live-book incl. 2 loser + 7 fill windows. Reported protocol / confirmed-dead-only / none variants; verdicts invariant.
 - 3 once per window: summarize2 = one row per armed window; per-rung tables count windows per rung and are labeled so; no tick or per-fill totals.
 - 4 walk-forward assertion: not applicable - T2 fits no model; only the deployed TWAP_MARGIN_P995 is used (identity asserted).
 - 5 day-block bootstrap: whole-ET-day resamples, B=5000, 26 unique day blocks reported next to every CI/p (H1 ON-OFF, H2-base, S1/S2-S0, H4 cells); adoption rules themselves are deterministic.
 - 6 ANTI + edge<0 control: ANTI run and reported for every H1/H2/H3/H4 cell; edge<0 bucket not definable for a binary-sign ladder - ANTI stated as that control (no omission).
 - 7 pre-declared cell first: drivers run k25/need 0.6/R8/$200 (H1), thr 0.05 k25 (H2), S0->S1->S2 at k25 ON (H3) before any other cell; section 0 quotes those cells first.
 - 8 literal bar diff: table in REPORT.md section 8; one ambiguity flagged - H3 'flip-fill loss on any relaxed rung' evaluated per window (per-rung-fill counting could only add losses, verdicts unchanged); H2 latch is a design choice (no-latch variant reported, same verdict). No threshold changed.
 - 9 copied before edit: repo ws2_ladder_replay.py md5 unchanged (fbce52f8...), git status shows no tracked change; edits only in scratch ladder_replay_v2.py; sys.dont_write_bytecode on the read-only import.
 - 10 forbidden-call grep over all T2 scripts (subscribe/websocket/ws:///wss:///place_order/cancel_order/place_gtc/polybot.main/run_polybot/HTTP clients): zero hits.
 - 11 book_p freshness: no book_p feature/baseline in T2; H2 triggers are BBO change events (fresh by construction, <=1 s trailing).
 - 12 table identity: r1_tables()['P995'] == TWAP_MARGIN_P995 asserted True in t2_tests.py.
 - 13 real multi-arm windows: 134 era windows with >=2 MAKER LADDER lines in docs/audit/data logs (113 'sign inside noise', 56 'projection cold', 6 'projection flipped' before a re-arm; 0 fills on arm>=2 in that period); harness cross-check at the then-deployed config: 34/66 floor re-arms reproduced, first-arm timing within +-1 s for 80%.
 - 14 refusal-logic diff + unit tests: production refuses only on has_open_or_pending_market/has_position_for_market (open/pending_resolution) = harness 'booked arm blocks'; tests: partial fill blocks re-arm PASS, zero-fill floor cancel re-arms PASS, flip re-arms same tick PASS, sub-$1 unbooked fill dropped PASS.
 - 15 zero-delta inspection: 6,105 zero-delta windows = 6,041 single-arm + 64 multi-arm without a later fill + 0 multi-arm with a later fill; no blind-replay pattern.
 - 16 trailing-only H2 classifier: re-derived; trailing-only (rx<=t) and +-1 s forward-peeking agree on all 4 windows (3 losses + live loss); the 4th 'known loss' 1788267900 is not a bot-zone loss under the bridged sign.
 - 17 thresholds 0.03/0.05/0.08: no window's classification flips (1787620800 changes trigger kind at 0.08, still fires before the fill).
 - 18 H3 per-window tallies: loss counts and relaxed-rung flip-fill losses are per window; per-rung tables separately labeled.
 - 19 H4 labeled non-adoptive/pending 09-11 in the report header, table and JSON; no H4 number appears in H1/H2/H3 totals.
 - 20 certain_winner close+5 s gate applied (pc_mode='capture'): 702/6,107 armed windows fail closed, 0 post-close fills in any run, winner-side dollars unchanged (+$995.31 both modes).
 - 21 AT_PRICE_QUEUE_SH=135 flagged as an unverified extrapolation on the 0.15/0.10/0.05 rungs (measured 08-17 on 0.80..0.20 levels); shared by S0 so it does not favor S1/S2.
 - 22 hold-out 1788267900: does NOT reproduce as a loss under re-arm ON (bridged r toward Up peaks 0.58; armed Down k 17.2, the winner, no fill); book-dynamics classified it with the plain projection.
GAPS:
 - Second-half (08-26..09-07) re-arm evidence is empty at k_max 25: no loser or winner re-arm fill under any exclusion; the H1 rule cannot pass or be refuted on the second half without more days.
 - Harness cancels/placements are evaluated at raw ticks (~1 Hz); production's maintain() runs at 4 Hz on a continuously drifting projection and re-armed ~2x as often in the logs (34/66 reproduced) - re-arm exposure here is a lower bound.
 - H2 'no latch' variant re-arms at the next raw tick; production's hot path could re-arm within the same iteration on a book wake, making the rule even weaker than modeled.
 - certain_winner replica is only meaningful from 08-18 (recorded t stream before that is the retired 30 s topic: 16 capture-vs-label disagreements, all 08-14..17); changed no dollars.
 - Paper fill rule (strictly-below full fill; 135-share at-price credit) remains unvalidated against live ladder fills (0 live fills post-era); every dollar is paper-rule dollars.
 - Tape captures 8-40% of post-close volume; 0 post-close fills in every run, so no post-close dollars are claimed either way.
 - dead_book_windows.csv defect is a Phase-1 artifact used by every Phase-2 test; T2 verdicts are invariant to it, T1/T3/T4 may not be - recommend rebuilding it from BBO activity (dead_confirmed.csv, 225 windows, is a drop-in candidate).
 - Log cross-check covers only 08-18..08-27 (the audit-log span); no production logs for September were available locally to validate re-arms at the deployed need 0.6 / k_max 25.
RECOMMENDATION: Adopt none of H1/H2/H3 under the pre-registered rules: H2 (reprice cancel) and H3 (S1/S2) fail decisively and should be logged as dead ends; H1 ("one arm per window") fails only on a degenerate second half (0 vs 0), while every re-arm loss in 26 days (-$475 across 4 windows, all 08-20..08-25) sits in re-arms and every re-arm win is +$6.25 - the operator should treat the re-arm defect as real, live-validated by 134 logged multi-arm windows, and either carry the H1 rule forward to a re-decision once September accumulates re-arm events, or make a policy call outside the pre-registration knowing the harness under-counts production re-arms (34 of 66 logged). Separately and urgently for the program: rebuild book-dynamics/dead_book_windows.csv from BBO activity (dead_confirmed.csv, 225 windows, is a drop-in) before T1/T3/T4 rely on it, since the current list removes 28 live-book windows including 2 loser windows and 42% of the baseline replay dollars. Nothing here changes what trades today; the deployed S0 / k_max 25 configuration remains the operative one.
====================================================================================================
## None
REPORT: C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/T3-wallets-oos/REPORT.md
HEADLINE: T3 re-tested, fresh out-of-sample (09-02..09-07 ET, 542 windows, 28,683 wallet-window observations after dead-book exclusion), whether the "top-decile prior-P&L, one-sided" wallet class that looked informative in Phase-1's in-sample/selected corpus still beats the book. The pre-declared cell (B5a) came back NEGATIVE (-0.72pp, day-clustered t=-1.28), triggering the pre-registered KILL rule. This is not a flat null: the first 3 fresh days alone would have passed B5a's own bar (+0.41pp, t=2.76) while the last 3 collapsed (-2.40pp), and the pre-declared bottom_directional control (expected <=0) came back significantly POSITIVE (+0.86pp, t=2.99, bootstrap p=0.000) -- the class ranking looks inverted out of sample this week, not merely uninformative.

BARS:
 - [FAIL] B5a: measured=exc=-0.0072 (-0.72pp), t_day=-1.279, n_days=6, n_obs=28683, n_win=542; bootstrap (B=5000, unweighted) P(mean<=0)=0.914 | threshold=top_directional BUY excess (win rate - book price) at k>60, pooled: >0 with day-clustered t>=2.0 over >=5 fresh ET days | Pre-declared cell, computed first as required. Kill bar (t<1.0) literally triggered. Book-proxy sensitivity: primary (30s/complement-allowed, 93% coverage) is negative; STRICT two-sided <=10s proxy (23% coverage) is positive (+1.31pp, t=1.75, still <2.0) -- sign is not proxy-invariant, disclosed not resolved. Chronological half 1 (09-02..04) alone would pass (t=2.76); half 2 (09-05..07) alone drives the kill (t=-28.7 on only 3 day-clusters -- treat magnitude with n_days<5 caution).
 - [UNDECIDABLE] B5b: measured=k(25,60]: exc=+0.87pp t_day=1.39 n=6200; k<=25: exc=+0.97pp t_day=1.15 n=1109; k<=25 EXACT-tape-match-only: exc=+2.80pp t_day=2.74 n=442 (pre-mortem #16) | threshold=none pre-declared (reported, not gated): same class/side at k in (25,60] and k<=25 | No pass/fail bar exists in the pre-registration for B5b -- descriptive context only, consistent with its own text. All three cells sit near or above the informal 2.0 line but on far fewer observations/windows than B5a; not adopted as confirmatory given B5a's own kill.
 - [FAIL] B5c: measured=k=0-6: net=-0.71pp (t=-4.22); 6-25: net=-0.76pp; 25-60: net=-0.51pp; 60-90: net=-0.42pp -- ALL FOUR buckets net-negative after fee, n_obs=120/1035/6200/6504 | threshold=none pre-declared (reported, not gated): follower buys at ask+2.5s, k<=90, net of the 0.07*p*(1-p) fee | No horizon shows a net-of-fee positive follower edge -- decisively not a buildable follower strategy on this signal, independent of the (unmeasured) data-api indexing lag caveat carried from Phase-1 (N=4, unverified).
 - [FAIL] B5d: measured=k>60: exc=+0.86pp (+0.0086), t_day=2.99, n_obs=25722, n_win=542; bootstrap (B=5000) P(mean<=0)=0.000; class attrition: 352/483 (73%) of the frozen bottom decile traded at all in the fresh window | threshold=bottom_directional BUY excess <=0 (pre-declared edge<0 control class) | Control was expected to show <=0; instead shows a significant POSITIVE excess -- the control itself fails, and does so in the direction opposite the frozen label (mirrors B5a's own inversion, not independent noise).
 - [FAIL] persistence: measured=N=3444 wallets, rho=-0.0569, p=0.00083 | threshold=descriptive (no numeric bar): Spearman(prior_pnl frozen 09-01, fresh mean BUY excess at k>25), wallets with >=5 prior windows & >=10 fresh BUY obs | Opposite sign from Phase-1's in-sample rho=+0.37 (N=3,056); magnitude is small but the large N makes it statistically distinguishable from zero and from the original sign. Consistent with the B5a/B5d inversion: the 09-01 P&L ranking did not carry forward.
 - [FAIL] kill/pass: measured=B5a t_day=-1.279 (<1.0); pass_bar_met=False; kill_bar_met=True | threshold=Kill: B5a day-clustered t<1.0 -> 'wallet information not confirmed out of sample'. Pass: B5a's full bar met (exc>0, t>=2.0, n_days>=5) -> escalate on-chain identity latency as a build proposal; nothing trades off this test either way. | KILL triggered on the literal, pre-declared cell. No infra build is warranted by this run. The one clean confound found and disclosed: this slice's own task instructions said '>=0.4s' request spacing while the frozen doc says '>=0.5s' -- t3_pull.py ran at 0.4s (flagged per pre-mortem #8; an operational parameter, not a statistical threshold, so it cannot have produced this result).
KEY FACTS:
 - Classes frozen BEFORE any fresh pull, from wallets/joined_trades.parquet (362 windows, 20 ET days, through 2026-09-01 only): 19,665 wallets classified (top_directional=388, bottom_directional=483, two_sided_mm=456, wall_camper=700, six_cluster=6, other_known=17,632); frozen_classes.json sha256=ed7f245b...c898, re-verified byte-identical after the fresh pull completed (pre-mortem #13, no leakage possible).
 - Fresh pull: 576 windows selected by a fixed stride-3 chronological rule (index%3==0 on window_labels, no outcome/volatility filter) out of 1,726 in ET 09-02..09-07; 572/576 returned rows; full paging with no offset ceiling other than a task-directed 20-page/10,000-row safety cap, hit by 7 windows (logged, not silent); max offset reached 9500, confirming the retired scripts' offset<=3000 cap was the real defect.
 - Join covers 2,771,093 raw rows -> 562 windows after excluding 10 pulled windows flagged by an independently-built dead/frozen-book detector (36/1678 labeled windows in range, 2.15%, flagged on placeholder-book or <=1-BBO-record signatures); 2,578,715 rows have a book quote (93.05% coverage: 71% micro BBO/tape-VWAP proxy split roughly 23%/70%, 7% unresolved).
 - B5a (pre-declared cell): top_directional BUY excess at k>60, pooled = -0.72pp (t_day=-1.28, n_obs=28,683, n_win=542, 6 ET days) -- the KILL bar (t<1.0) is literally met.
 - B5d control (bottom_directional BUY, expected <=0): +0.86pp (t_day=2.99, bootstrap P(<=0)=0.000, n_obs=25,722) -- the control itself is significantly positive, opposite its expected sign.
 - ANTI (top_directional SELL, k>60): -1.69pp (t_day=-2.24) -- still points the 'informed' direction even though the same class's BUYs (B5a) do not.
 - Persistence Spearman (fresh days, N=3,444 wallets with >=5 prior windows & >=10 fresh BUY obs at k>25): rho=-0.057, p=0.0008 -- opposite sign from Phase-1's in-sample rho=+0.37 (N=3,056).
 - B5c follower economics (buy at ask+2.5s, top_directional BUY signal, k<=90): net of the 7% taker fee, all four horizon buckets (0-6/6-25/25-60/60-90s) are NEGATIVE (-0.71/-0.76/-0.51/-0.42pp) -- no buildable follower edge on this signal at any tested horizon.
 - Desk check, empirically verified (not just inferred): decoded a real fresh-window OrderFilled log on-chain (tx 0x9c5bd9a7...938c7, via free public Polygon RPCs 1rpc.io/matic and polygon-bor-rpc.publicnode.com) and confirmed the maker/taker addresses match the data-api proxyWallet field byte-for-byte for both counterparties; CTF Exchange = 0xE111180000d2663C0091e4f400237545B87B996B; OrderFilled(bytes32 orderHash, address maker, address taker, uint256 makerAssetId, uint256 takerAssetId, uint256 makerAmountFilled, uint256 takerAmountFilled, uint256 fee).
 - Book-proxy sensitivity: the strict two-sided, <=10s-only book construction (23% of rows) flips B5a's sign to positive (+1.31pp, t=1.75, still short of the 2.0 pass bar) -- the primary result's sign is not robust to book-proxy choice, disclosed rather than hidden.
PREMORTEM CHECKS DONE:
 - 1. rx-clock check: confirmed chainlink_feed.py running_avg/projected_final_twap use only the receipt clock, never payload_ts; T3 needs no projection and its own k uses the trade's exchange-match clock (tape ets or dapi_ts-2.3s) -- grepped own scripts, zero payload_ts usage.
 - 2. Independent dead/frozen-book detector built directly from the micro tape (window_paths.db doesn't cover these dates): 36/1678 windows flagged and excluded; cross-checked against book-dynamics/dead_book_windows.csv (that file's range unexpectedly overlaps this fresh window because it wasn't date-bounded to 09-01 -- explained, does not leak into wallet-classification freshness).
 - 3. Every $/win-rate/edge counted once per (wallet,window,side,token,bucket) observation as pre-registered; n_obs>n_win is by design (many wallets per window), reported side by side everywhere.
 - 4. No day-d-or-later row enters classification for day d: verified by construction (freeze used only through 09-01) and by byte-identical re-freeze after the pull.
 - 5. Day-block bootstrap B=5000 (>=2000 required), n_days reported next to every bootstrap p-value (6, or 3 for chronological halves).
 - 6. ANTI (top_directional SELL) and edge<0 control (bottom_directional BUY, B5d) both computed and reported for every claimed cell.
 - 7. Pre-declared cell (B5a) computed and printed first in t3_measure.py; verdict quotes it directly, not a best grid cell.
 - 8. Every numeric bar diffed against the literal pre-registration text; one deviation found and disclosed: request spacing run at 0.4s vs the frozen doc's 0.5s (operational parameter, cannot bias results, flagged not hidden).
 - 9. ws2_ladder_replay.py not touched -- N/A, T3 uses no projection/margin table.
 - 10. Grepped all T3 scripts for subscribe/websocket/place_order/cancel_order/polybot.main/run_polybot: zero hits besides this check's own descriptive text.
 - 11. book_p freshness: primary proxy is looser than the deployed 10s two-sided gate by design; a strict 10s two-sided sensitivity re-run was added and reported (flips B5a's sign, see B5a note).
 - 12. p99.5/margin table check: N/A, no margin table used in T3.
 - 13. Regenerated top/bottom_directional using only data through 09-01 AFTER the fresh pull completed and diffed byte-for-byte (sha256 identical) against the frozen file used for the pull -- zero leakage.
 - 14. Logged max offset per window (max 9500) and paging stop reasons (569 empty_page, 7 MAX_PAGES_HIT -- the task-directed safety cap, logged not silent); confirms the offset<=3000 defect Phase-1 found is closed.
 - 15. Backward-only VWAP recomputed vs the symmetric +/-3s/+/-10s window: gap is -0.0046pp (symmetric is LOWER, not higher) -- no evidence of momentum leakage from future prints inflating the result.
 - 16. k<=25 recomputed on exact-tape-matched rows only (dropping the dapi_ts-2.3s estimate): exc=+2.80pp, t=2.74, n=442 -- reported, not treated as confirmatory given the small N.
 - 17. Confirmed the stride-3 selection is a fixed epoch-order rule (index%3==0 on chronologically sorted window_labels) fixed in selected_eps.json before any trade row was fetched -- no outcome/volatility/reversal filtering.
 - 18. two_sided_mm/wall_camper/six_cluster results on fresh data computed and explicitly labeled exploratory/not-gated, kept out of the B5a-only kill/pass decision.
 - 19. Desk check confirmed docs-only: only Gamma/data-api-adjacent doc pages, GitHub source files, and read-only Polygon JSON-RPC POSTs; zero CLOB/RTDS websocket connections opened.
 - 20. Bottom_directional frozen-class attrition reported: 352/483 (73%) traded at all in the fresh window -- B5d's control runs on this survived subpopulation, stated plainly.
 - 21. B5c's results (all negative) reported together with the unmeasured data-api indexing-lag caveat from Phase-1 (N=4, unverified, one probe showed minutes of lag) even though the negative result makes the caveat moot for a build decision.
 - 22. Persistence N (3,444) reported explicitly and flagged as far lower power than Phase-1's N=3,056, though large enough here to detect a small, sign-reversed effect.
GAPS:
 - Only 6 fresh ET days; the chronological-half split is only 3 day-clusters per half, so the half-2 t-stat's very large magnitude (t=-28.7) reflects unusually low cross-day variance on a tiny cluster count, not a stronger result -- do not over-read its magnitude.
 - Primary book-proxy result (negative) is not robust to a stricter two-sided <=10s book construction (which flips to positive but still short of the pass bar) -- the sign is genuinely proxy-sensitive on this data, unresolved by this run.
 - Data-api indexing lag beyond the measured 2.3s block-time offset remains unmeasured live (Phase-1's N=4 probe is the only evidence, showing possibly minutes of lag) -- B5c's negative result makes this moot for now, but it would need to be closed before any future wallet-identity build.
 - CLOB WS last_trade_price carrying transactionHash live is still unverified (no WS subscription opened, per the no-listener rule) -- the on-chain identity path's real-time latency vs the data-api is inferred from block time + RPC responsiveness, not measured end-to-end on a live window.
 - 48 of the 1,726 labeled windows in the fresh ET range had zero micro-tape/tape-print coverage at all (1678/1726 checked by the dead-book detector, 1626/1726 present in tape_prints.parquet) -- not separately investigated; likely low-volume windows, but not confirmed.
 - wall_camper 120-240s finding from Phase-1 (+3.2pp, t=9.5, flagged as possibly a favorite-underpricing book fact rather than wallet skill) was not re-tested at that specific horizon in T3 (only k>60 pooled was computed for the exploratory classes).
 - The request-spacing deviation (0.4s used vs 0.5s frozen) is disclosed but not corrected retroactively; a re-run at the literal 0.5s spacing was not performed given it cannot affect the measured statistics and would only have delayed the result by roughly 25%.
RECOMMENDATION: KILL stands on the pre-declared cell: do not build the on-chain identity infra proposal, and do not fold wallet identity into the entry-side engine on the strength of this run. But treat this as more than a null result to file away -- report the inversion explicitly to the operator. The class that looked like the strongest candidate in Phase-1 (top-decile prior-P&L, one-sided) traded WORSE than the book on BUYs this week while the pre-declared bottom-decile control traded BETTER, and the reversal is concentrated entirely in the second half of the fresh window (09-05..07) -- consistent with either genuine non-stationarity in wallet skill/P&L rank (mean reversion of "hot hands"), a regime shift coincident with the known 09-01 supply/rewards changes already tracked in RESEARCH.md, or the original in-sample finding being partly an artifact of Phase-1's small, reversal-selected corpus (68 of 362 windows were hand-picked). The one thread still pointing the expected direction -- top_directional's SELLs (ANTI, t=-2.24) -- is not strong enough on its own to justify a new build, but is a plausible, cheap next probe if the operator wants one: a dedicated, freshly pre-registered SELL-side-only test, not a re-reading of this run's BUY-side kill. Given REFUTATIONS.md's standing entry-edge-research-closed posture and this fresh, decisive, adversarially-checked negative, the wallet-identity data class should go back to closed status pending a genuinely new data class or a much longer (>=6 clean-day, ideally >=20-fill-equivalent) fresh window before any re-open.
====================================================================================================
## None
Report written to `C:/Users/abhat/AppData/Local/Temp/claude/C--Users-abhat-Personal-PolyBot/dc8ad5cd-5d51-4fc7-920c-a96d4dbabc90/scratchpad/engine_restart/critic/REPORT.md`.

Findings, most severe first:

1. **Wallet-identity edge inverts sign out of sample, on three independent statistics at once.** Phase-1's persistence Spearman +0.37 (N=3,056) becomes -0.0569 (p=0.0008, N=3,444) fresh; the primary class B5a comes back negative (kill) and its own edge≤0 control B5d comes back significantly positive (+0.86pp, t=2.99) — the frozen labels point backwards this week, not just uninformatively. `T3-wallets-oos/REPORT.md` §4, §8.

2. **T2's entire ladder-hygiene dollar quantification (H1/H2/H3) has zero real-world confirmation of a re-arm ever filling, at any configuration.** The only production-log validation of re-arms covers 07-13..08-27 under an old config (need 2.0→1.0, 5 rungs) and shows 0 fills on any arm≥2 in that whole period; every dollar behind the current H1/H2/H3 verdicts is a paper-replay artifact untested against the currently deployed settings (need 0.6, 8 rungs, k_max 25). `T2-ladder-hygiene/REPORT.md` §1.1, §7 item 13, §9.

3. **A shared upstream artifact defect (`dead_book_windows.csv`) is diagnosed by T2 but never cross-corrected in T1 or T4.** T2 shows 28 of 253 "dead" windows are actually live/decided books, including 2 of the 3 named ladder losses and 7 of 17 baseline fill windows (+$793.66); T1 and T4 each ran their own *additive* dead-book checks but neither tested restoring T2's specific 28 windows, so "T1/T4 are robust to this" has never actually been verified with the correction that matters. `T2-ladder-hygiene/REPORT.md` §2.3; `T1-zone-engine/REPORT.md` §2.6; `T4-whole-window-controls/REPORT.md` pre-mortem #2.

4. **Phase-1's roster of "known loss windows" was wrong, in two independent ways, and the frozen pre-registration was built on it.** The pre-mortem-mandated 4th hold-out loss (ep 1788267900) is NOT a real loss — spot-checked directly against `book-dynamics/proj_grid.csv` (bridged s stays 0.536/0.246/-0.473 at k=25/22/20, never clearing 0.6 inside k∈[6,25]; book-dynamics had used the plain, not bridged, projection). The real, previously-unflagged 4th loss is a different window (ep 1787678700, -$200) hidden by the dead-book CSV defect in item 3. `T2-ladder-hygiene/REPORT.md` §3.1, §3.3.

5. **Phase-1's "$1,932 loser-side reachable value" figure (cited in the pre-registration to motivate T2) is ~7x larger than the engine-true bookable loss in the same 3 windows.** Spot-checked against `information-structure/REPORT.md`'s own table: "$1,932" is raw print value at the resting price ("$ ceded"), not what the paper fill rule actually books; T2's replay of the same windows books -$275 total. The pre-registration's prose conflates the two quantities. `information-structure/REPORT.md` §3a/3e; `T2-ladder-hygiene/REPORT.md` §3.1.

6. **T3's kill is not robust to a plausible, more-standard book-freshness definition.** T3's primary book proxy (30s stale, complement-allowed) gives the kill (t=-1.279); the stricter two-sided ≤10s proxy that T1/T4 use elsewhere flips the sign (+1.31pp, t=1.745) into the pre-registration's own undeclared gap between kill (t<1.0) and pass (t≥2.0) — a different, reasonable methodological choice would have produced neither a kill nor a pass. `T3-wallets-oos/REPORT.md` §4 "B5a sensitivity re-runs."

7. **T1's B1 "kill at k≥25" is model-class-dependent, though it doesn't change the final disposition.** Under the pre-registered bucket model only k=58 fails (triggering kill); under D4's exact-k model class, k=58 also passes (all three k≥25 cells clear the bar). The overall T1 kill survives only because B2 independently fails on its own bars. `T1-zone-engine/REPORT.md` §2.5, §2.9.

8. **T1's taker-leg PASS bars are carried by a thin, non-representative slice** — 44% of the total dollars come from 8% of trades (26 where the book leaned against the model), and an unstated pricing convention (ask(t) vs ask(t+L)) flips the whole cell's sign; already fails on bars (ii)/(v) so the disposition is unaffected, but the PASSed sub-bars shouldn't be read as "close to working." `T1-zone-engine/REPORT.md` §3.1-3.2.

9. **T3's on-chain identity-verification claim ("EMPIRICALLY VERIFIED") rests on a single decoded transaction (N=1),** cross-checked against two RPCs agreeing on that one transaction, not on independent transactions. `T3-wallets-oos/REPORT.md` §6.

10. **Minor, self-disclosed, immaterial:** T3 ran its fresh pull at 0.4s request spacing vs the frozen doc's 0.5s (task-brief inconsistency, correctly flagged, doesn't bias any statistic). `T3-wallets-oos/REPORT.md` §3 item 8.

Operator needs before acting (from §5 of the report): rebuild the dead-book exclusion list from T2's `dead_confirmed.csv` and re-run T1's B1/B2 and T4's P1 against it; get September-dated logs (or a short observation window) at the *current* ladder config to confirm a re-arm can actually fill; resolve which "recalibrated book" (logistic vs isotonic/spline) is the pre-registration's real baseline, since it flips T1's only two PASS cells; and more calendar days before re-deciding T1's k=58 model-class dependency and T3's wallet kill (neither is fixable by more replay of existing data). No code change is indicated — every kill/fail across T1-T4 is independently over-determined despite the gaps above.
