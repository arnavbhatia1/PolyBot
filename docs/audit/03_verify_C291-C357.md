# 03 — Verification C291–C357 (§7 Hard rules, §8 Project layout, §9 Data sources, §10 Running + invariants, §11 Discord)

Verified 2026-08-27 against branch `main` (HEAD 4fc8f847; code paths cited as `path:line`),
`docs/audit/data/` (DB backups 20:27Z, logs 07-13→08-27), `scripts/research/data/vps-0821/`
reports, `docs/audit/02_data_facts.md`. Docs (CLAUDE/RESEARCH/REFUTATIONS/README) carry
zero weight — a claim backed only by them is UNVERIFIED.

§7 rules are prohibitions: VERIFIED-CODE means the code *embodies or enforces* the rule.
Rules the code neither enforces nor embodies are UNVERIFIED and listed at the end as
policy-only.

| id | disposition | evidence | note |
|---|---|---|---|
| C291 | VERIFIED-CODE | `requirements.txt` has no ML/stat library; `polybot/indicators/__init__.py` is `# (empty)`; `polybot/core/signal_engine.py:1-7` ("no spot prediction, no feature stack"); the only entry path is `main.py:895 _evaluate_signal_and_enter` → `SignalEngine` | Verified by absence. `gate_stats.json` still carries ML-era gate names — history from deleted code (02_data_facts). |
| C292 | VERIFIED-DATA | `scripts/research/data/vps-0821/h2_report.md` VERDICT: REFUTED — N+1 open mispricing, held-out EW −2.13..−4.43¢/sh at every δ, 1,972 windows, 60s era 08-14→08-21; book prices the strike within 2s of open | Only the open seam has 60s-era data; "everywhere" beyond that rests on REFUTATIONS.md (doc, zero weight). |
| C293 | VERIFIED-CODE | `chainlink_feed.py:219-231 projected_final_twap` = w·A + (1−w)·spot over the observed 60s span; `signal_engine.py:1-7` "the ONLY signal source"; the ladder hook reuses it `main.py:1073-1079` | The `bridged=True` variant adds a Binance delta to the spot term (`:227-231`) — still no model, but a nowcast term, not purely "already observed". |
| C294 | UNVERIFIED | Policy statement; no code embodiment beyond C291's absence; nothing to grep | Policy-only. |
| C295 | CONTRADICTED | As an invariant: live preflight `main.py:2462-2475` checks auth/balance/allowance only — no bar read; `kill_rule_tripped`/`live_health_read` are consumed only by the nightly ping (`main.py:2772-2780, 2825, 2958`). Data: `polybot_live_audit.db` holds 1 `lock_dip` live fill 08-13 (+$0.47) and 5 `deep_proj` live fills 08-14→08-15 (−$34.18) while every paper validation epoch is ≥ 08-19 (02_data_facts) | Holds only as operator policy; the 08-13/14 live fills were deliberate "probes", which is exactly what the rule as written forbids. |
| C296 | UNVERIFIED | No code pins bar values; `loader.py:78-150 _check_range` bounds knobs, not bars. `signal_engine.py:29-31` restates the rule as a comment | Policy-only. Direction of the 08-27 re-fit was stricter (knots 2-4× wider, commit 03349951), not relaxing. |
| C297 | UNVERIFIED | Definition/policy; backed by RESEARCH.md:5-6 and the `signal_engine.py:29-31` comment only | Policy-only. |
| C298 | VERIFIED-CODE | `maker_bid.py:138-139` one `side` per window, `:164 place_gtc_bid` only; `live_trader.py:643-648 place_gtc_bid` posts `side=BUY` GTC; no GTC SELL/ask placement exists anywhere (grep `SELL` → FOK exit paths only) | The code is structurally one-sided. |
| C299 | UNVERIFIED | "Oracle-cadence trading" has no code referent; the pre-gate wakes on Chainlink reports (`main.py:2208-2213`) to evaluate the lock, which is not that | Policy-only. |
| C300 | VERIFIED-CODE | `market_scanner.py:45 symbol="btc"`, `:71-72 _make_slug → f"{symbol}-updown-5m-{ts}"`; `main.py:2449-2455` builds one scanner with no symbol override; `chainlink_feed.py:575` drops any payload not `btc/usd` | One market family is hard-wired end to end. |
| C301 | EXPIRED | REFUTATIONS.md:36-39 "Multi-market expansion (08-12, 24 windows/family): btc-15m 7.3 post-close sh/window, eth-5m 1.8, xrp-5m 0.6, sol-5m 0.0" — measured 08-12 on the 30s stream | Pre-08-14 origin; the claim itself labels it 30s-era. No 60s-era sibling data exists in the audit set. |
| C302 | UNVERIFIED | RESEARCH.md:152-155 only; no research script targets a sibling slug (grep `eth-updown|sol-updown|xrp-updown|15m` over `scripts/research/*.py` → none) | Consistent with "unmeasured" but doc-only. |
| C303 | UNVERIFIED | Policy; REFUTATIONS.md:39 restates. Code has a single btc scanner (C300) but nothing forbids adding one | Policy-only. |
| C304 | VERIFIED-CODE | `signal_engine.py:141` gates on the executable `ask` (no `mid` in signal_engine); `main.py:1478` reads the WS book, explicitly not `/price`. The only mids: `paper_trader.py:173-176` (spread-% sanity) and `live_trader.py:1719-1724` (resolution-price inference for orphan exits) | Neither mid use is edge math. |
| C305 | VERIFIED-CODE | `base.py:160-162 taker_fee = rate·shares·p·(1−p)`; applied at open `base.py:335`, close `:482,:537`, paper resolution `paper_trader.py:269,274`, taker entry `main.py:1360`, Kelly `signal_engine.py:141` | Maker fills deliberately book `fee_in_shares = 0.0` (`base.py:~411-415`, "makers are never charged") — a documented exception, not a skip. |
| C306 | VERIFIED-CODE | `base.py:140 DEFAULT_FEE_RATE = 0.07`; `market_scanner.py:210-217 fetch_fee_rate` returns the constant (no live fetch) | The exchange's true rate is not re-verifiable from local data (tape `fee_bps` is recorded, `recording.py:530`, but no tape is in the audit set). |
| C307 | VERIFIED-CODE | `base.py:144 EFFECTIVE_FEE_PEAK = round(0.07·0.25, 5)` = 0.0175; sole use `main.py:1593 effective_cost = spread/2 + EFFECTIVE_FEE_PEAK` | Flat-additive cost gate only. |
| C308 | VERIFIED-CODE | `EFFECTIVE_FEE_PEAK` has exactly one call site (`main.py:1593`, flat gate); every dollar fee goes through `taker_fee` (C305) | `signal_engine.py:151-159 _kelly` uses a third form — `net_b = b·(1−0.07)`, a proportional odds haircut, neither curve nor flat. Flag for the §3 verifier. |
| C309 | VERIFIED-CODE | `base.py:489, 540 gain_pct = pnl / position["size"]`; `outcome_reviewer.py:64`; `bot.py:177`. `log_return` still computed (`base.py:10, 481, 543`; `live_trader.py:1590`) and stored on `TradeResult`/outcome records (`main.py:878`) | log_return is telemetry only — no reader in `analyze_late_window.py`, `sniper_shadow_status.py`, or the Discord stats (grep). |
| C310 | VERIFIED-CODE | Both sizing paths multiply by `breaker.kelly_multiplier`: taker `main.py:1145-1147`, ladder budget `main.py:1076-1078`; tests `test_circuit_breaker.py`, `test_decision_parity.py` | `base.py open_trade` does not re-apply it — one application point per path, no bypass found. |
| C311 | UNVERIFIED | No code guard against deletion. Both DBs are git-tracked (`git ls-files polybot/db` → `polybot_live.db`, `polybot_paper.db`) and committed nightly (`run_polybot.sh:42`) | Policy-only; a deletion is recoverable from git history. |
| C312 | VERIFIED-CODE | `main.py:2025 trading_loop`, `:895 _evaluate_signal_and_enter` (gates), `:1073-1090` ladder hook (`_MAKER_MGR.consider_placement`), `:2731 _sniper_health_job` | — |
| C313 | VERIFIED-CODE | `ls polybot/config` → `settings.yaml`, `loader.py`, `.env`, `.env.example`; `loader.py:165-167` is the only YAML load in the codebase (grep); `run_polybot.sh:27` reads `mode:` from the same file | Secrets live in `.env`, not config values. |
| C314 | CONTRADICTED | `signal_engine.py:16` "60s-rule tables, re-fit 2026-08-27"; `:19-20` corpus "3,695 real-final windows (08-14..27, 15 ET days)"; commit 03349951 (2026-08-27) "Margin tables re-fit on 14 real-final days: every knot 2-4x wider" | The file holds the tables + `twap_margin` lock math (`:33-59`), but the "freeze 08-18" date is stale. |
| C315 | VERIFIED-CODE | `chainlink_feed.py`: sixty topic `:530/:612`, `_record_boundary:417` (strike), `projected_final_twap:219`, `spot_bridge_delta:187`, `RAW_GAP_MAX_S:40`; `clob_ws.py`, `market_scanner.py` present | `_json.py/_socket.py/_staleness.py` helpers also live here (unlisted). |
| C316 | VERIFIED-CODE | `recording.py:56 WindowPathRecorder`, `:504 TapeRecorder`, `:557 MicroTape` (+ `compress_recordings_job:698`, `cleanup_job:792`) | — |
| C317 | VERIFIED-CODE | `base.py:140-173` fee math; `paper_trader.py:16`, `live_trader.py:356`, `maker_bid.py:1` (`signal_leg="deep_proj"`), `circuit_breaker.py:32` | `correlation.py` (`concurrent_multiplier`) is also here, unlisted. |
| C318 | VERIFIED-CODE | dirs `agents/ memory/ discord_bot/` exist; `db/models.py:3` "Per-mode SQLite database (polybot_paper.db / polybot_live.db)" | `window_labels` DDL/writes live in `recording.py:128-144, 317` (same per-mode DB); `models.py` contains no label code (grep `label` → none). |
| C319 | VERIFIED-CODE | `run_polybot.sh:2-3` "THE supervisor (bot runs on the VPS under systemd)"; `scripts/polybot.service` `ExecStart=… run_polybot.sh`, `Restart=always`. 02_data_facts host section: `journalctl -u polybot` shows the unit live on the box | — |
| C320 | VERIFIED-CODE | `analyze_twap_lock.py:1-20` docstring; `HORIZON_S = 60.0` (`:57`); `mechanism_check:307` (served price_to_beat/final vs t-record, bit-exact); `TWAP_SWITCH_TS = 1786665600` (`:52`) | — |
| C321 | VERIFIED-CODE | `analyze_late_window.py:64 live_health_read`, `:198 mechanism_read`, `:254 queue_depth_read`, `:333 resolution_snapshot_read` | Its `TWAP_SWITCH_TS = 1786060800` (08-07, `:30`, used `:370`) differs from analyze_twap_lock's 08-14 value — flag for consistency. |
| C322 | VERIFIED-CODE | `ls scripts` → `sniper_shadow_status.py`, `verify_keys.py`, `smoke_order_test.py`, `smoke_gtc_test.py`, `reset_paper_clean.py` all present | — |
| C323 | VERIFIED-CODE | `scripts/research/README.md` present; `scripts/research/.gitignore:1 data/` (`git check-ignore` confirms, 0 tracked files under `data/`); census `ws3_census.py`/`r5_census.py`, error tables `ws1_measure60.py`/`ws1_freeze_tables.py`, engine-true grid `ws2_ladder_replay.py`/`r23_ladder_grid.py` | The repo-root `.gitignore` does not mention research data; the ignore is the nested file. |
| C324 | VERIFIED-CODE | root `ls` → `REFUTATIONS.md`, `RESEARCH.md`, `WALLETS.md` | Also `AUDIT.md`, `CLAUDE.md.archive-2026-08-27` at root (unlisted). |
| C325 | VERIFIED-CODE | `clob_ws.py:23 WS_URL`; `market_scanner.py:199 /book`, `:227 /tick-size`, `:272 /price`, `:288 /spread` | `main.py:1478` deliberately avoids `/price` for executable pricing. |
| C326 | VERIFIED-CODE | books: `main.py:1470-1490 _get_book` off `clob_ws`; tape: `TapeRecorder.on_trade` ← `clob_ws.on_trade` (`main.py:2661-2665`, `recording.py:518`); executable prices from WS BBO (`main.py:1478-1490`) | — |
| C327 | VERIFIED-CODE | `market_scanner.py:84 GET {GAMMA_API}/events?slug=`; `:94 GET /events/slug/{slug}` fallback, latched `:65, :89-90` | `live_trader.py:1670` also hits `/events/slug/` directly for reconcile. |
| C328 | VERIFIED-CODE | discovery `market_scanner.py:344-350`; resolution `main.py:1673-1690 _resolved_exit_price` via `event_metadata.final_price/price_to_beat`; labels `recording.py:214, 303-323` → `window_labels` | — |
| C329 | VERIFIED-CODE | `chainlink_feed.py:27 RTDS_WS_URL = "wss://ws-live-data.polymarket.com"` | — |
| C330 | VERIFIED-CODE | `chainlink_feed.py:527 crypto_prices_chainlink`, `:530 crypto_prices_twap_sixty`, `:540 crypto_prices` (btcusdt) | A fourth subscription, `crypto_prices_twap_thirty` (`:535`, `:604-610`), is record-only (§6's t3 tape) — the §9 table omits it. |
| C331 | VERIFIED-CODE | strike: `ingest_sixty:378-390` → `_record_boundary:417`; resolution watch: `recording.py:604-627 on_twap` (TAPE VERDICT) + `_check_resolution_source:325`; orphan fallback resolves only from boundary captures (`main.py:1823`, `strike_reliable:299`) | The exit price itself is Gamma-first (C328); the sixty stream is the verification/fallback source. |
| C332 | VERIFIED-CODE | `chainlink_feed.py:613-614` non-TWAP `btc/usd` → `ingest_raw:397`; `projected_final_twap:219-252` and `running_avg:151` read the raw ring under the `RAW_GAP_MAX_S` guard | — |
| C333 | VERIFIED-CODE | `chainlink_feed.py:558-573` routes `crypto_prices/btcusdt` into `ingest_binance` only ("never touches price/strike state"); `self._binance` is read solely by `spot_bridge_delta:187-200`; the other consumer is recording (`main.py:2671 on_spot → micro_tape.on_bz_tick`, `recording.py:641-649`) | `projected_final_twap:227-231`: the taker never uses the bridged variant. |
| C334 | UNVERIFIED | Systemd unit exists (`scripts/polybot.service`, `User=ubuntu`, `/home/ubuntu/PolyBot`) and is live on the box (02_data_facts host section: host `polybotvcn`, `journalctl -u polybot`). Nothing in code pins the host; "Oracle Stockholm" appears in no code or data file (grep) | "ONLY" is policy (the lock is per-host, C342); the datacentre location is doc/memory only. |
| C335 | VERIFIED-CODE | `settings.yaml:191-192` start 0:01 → `main.py:2046 sched_start_et` → `_check_trading_schedule:1969 in_trading_hours`; supervisor wakes at `tomorrow 00:01` ET (`run_polybot.sh:71`) | — |
| C336 | VERIFIED-CODE | `settings.yaml:193-194` end 23:30 → `main.py:2047` → `:1969`; supervisor wind-down band ≥ 23:30 (`run_polybot.sh:58, 73`) | — |
| C337 | VERIFIED-CODE | `settings.yaml:198-199` hour 23 / minute 45 → `main.py:2546-2547` → `scheduler.py:84-89` fires in [23:45, 23:50) ET. Data: auto commits land 04:03–04:18Z (= 00:03–00:18 ET) 8/8 nights 08-20→08-27 (`git log --grep "auto: daily"`) | — |
| C338 | VERIFIED-CODE | `run_polybot.sh:41-46` (`code -eq 0` → `git add … && git commit && git push origin main`, one retry). Data: 8 "auto: daily pipeline update" commits 08-20→08-27 | — |
| C339 | VERIFIED-CODE | `run_polybot.sh:65-79` sleeps to 00:01 ET, loop top `:21 git pull --rebase --autostash origin main`, `:35` restart | 12:01, not 00:00 exactly. 02_data_facts: 08-27 06:45Z restart was `needrestart` after an apt upgrade, not the supervisor — an unscheduled restart path the doc does not mention. |
| C340 | VERIFIED-CODE | `run_polybot.sh:56-62` (`code -ne 0` and ET < 23:30 → `sleep 60; continue`) | — |
| C341 | UNVERIFIED | No hostname/platform check in `main.py` (grep); the socket lock is per-host. README.md:50 restates (doc) | Policy-only. |
| C342 | VERIFIED-CODE | `main.py:3095-3106 _acquire_single_instance` binds `127.0.0.1:49653` (loopback — invisible to any other host); `run_polybot.sh:7, 31` pkill is local too | — |
| C343 | VERIFIED-CODE | `scripts/verify_keys.py:13-16` runs `verify_auth`; `scripts/smoke_order_test.py:79-84` refuses to post without `--confirm` | The sequence is operator procedure, not enforced; live boot independently runs `verify_auth` (`main.py:2470`). |
| C344 | VERIFIED-CODE | `models.py:185, 249` UTC isoformat writes; `models.py:431-432` ET day → UTC range for stats; `main.py:19 ET` for schedule/windows; `recording.py:550, 691` UTC dates; no naive `datetime.now()` in `polybot/` (grep). Data: all 38 paper `positions.entry_timestamp` carry `+00:00` (08-18→08-27); `window_labels.labeled_at` is epoch | — |
| C345 | VERIFIED-CODE | `.gitignore`: `polybot/memory/recordings/`, `polybot/db/window_paths.db*`, `polybot/db/late_window_collect.db*` | `memory/state` and `memory/outcomes` are NOT ignored (they commit — C346). |
| C346 | VERIFIED-CODE | `run_polybot.sh:42 git add polybot/config/settings.yaml polybot/memory polybot/db`. Data: commit d1f1c880 (08-25) touches `polybot_paper.db`, `memory/outcomes/*.json`, `memory/state/*.json`; 8 nightly commits 08-20→08-27 | — |
| C347 | UNVERIFIED | Policy (README.md:6-7 restates). Counter-evidence: the box runs `analyze_twap_lock.ladder_recalibrate` nightly (`main.py:2716 _maker_ladder_job`, 480s budget) and it was "abandoned (600s)" 4 nights 08-23→26; kernel OOM killed an interactive `python` analysis on the box 08-13 17:16Z (02_data_facts) | Policy-only, and not consistently honoured. |
| C348 | UNVERIFIED | No code blocks `mode: live` on a bar (`main.py:2462-2475` preflight = auth/balance/allowance); `live_health_read`/`kill_rule_tripped` feed only the alert-only nightly ping (`main.py:2772-2780, 2825, 2958`) | Governance policy; see C295 for the data showing live fills preceded any passed bar. |
| C349 | VERIFIED-CODE | `discord_bot/bot.py:69-70 @bot.command(name="status")` | — |
| C350 | VERIFIED-CODE | `bot.py:158-159 @bot.command(name="history") async def history(ctx, n: int = 10)` | — |
| C351 | VERIFIED-CODE | `bot.py:195-198` sets `bot.is_paused = True` | — |
| C352 | VERIFIED-CODE | `bot.py:200-203` sets `bot.is_paused = False` | — |
| C353 | VERIFIED-CODE | `bot.py:205-233 clear_channels(ctx, target="all", confirm="")`; accepts `trades|control|all`, requires literal `confirm` token, purges Discord chat only | — |
| C354 | VERIFIED-CODE | `bot.py:257-258 @bot.command(name="session")` → `send_session_banner` | — |
| C355 | VERIFIED-CODE | `bot.py:235-236 @bot.command(name="pipeline")` | Hard-codes 23:45 for display (`:239`) instead of reading `agents.daily_pipeline_*`. |
| C356 | VERIFIED-CODE | `bot.py:57-58 @bot.command(name="commands")` | — |
| C357 | VERIFIED-CODE | `bot.is_paused` (`bot.py:197`) is read only via `main.py:2997 is_paused_fn` at `main.py:2136`, inside `_entry_pass`, returning before discovery/signal/ladder placement ("positions still managed" `:2135`); resolution and orphan management run outside that gate; `maker_bid.py` never reads it | Pause also stops NEW ladder rungs (hook is inside the gated path, `:1079`) but does not cancel rungs already resting. |

## Summary

| disposition | count |
|---|---|
| VERIFIED-CODE | 52 |
| VERIFIED-DATA | 1 |
| CONTRADICTED | 2 |
| EXPIRED | 1 |
| UNVERIFIED | 11 |
| **total** | **67** |

**CONTRADICTED**
- C295 — "No deployment before a kill bar passes" is not a system property: live preflight checks auth/balance only, and the live DB holds 6 real fills 08-13→08-15 (1 lock_dip, 5 deep_proj, −$33.71 net) before any paper validation epoch (all ≥ 08-19).
- C314 — signal_engine.py header says "re-fit 2026-08-27" on 3,695 windows (commit 03349951); the layout line's "60s-rule freeze 08-18" is stale.

**EXPIRED**
- C301 — the sibling post-close refutation is the 08-12 measurement (REFUTATIONS.md:36-39), 30s stream; no 60s-era sibling data exists.

**Policy-only prohibitions (no code embodiment; hold as operator rules, not system properties)**
- C294 measurement-in-scope / prediction-out-of-scope
- C296 never relax a bar (direction of the one re-fit was stricter)
- C297 re-measurement ≠ relaxing (definition)
- C299 no oracle-cadence trading (no code referent)
- C303 scaling is SIZE on one book
- C311 don't delete `polybot_*.db` (git-tracked, so recoverable)
- C341 never run on a workstation (lock is per-host, no host check)
- C347 heavy analysis never on the box (a budgeted nightly recalibration does run there and overran 4 nights; one 08-13 interactive analysis was OOM-killed)
- C348 kill bars are the deployment authority (nothing gates `mode: live` on a bar)
- C334's "ONLY on the VPS" clause (the systemd unit is real; the exclusivity and "Oracle Stockholm" are not verifiable from code or data)

**Notes worth carrying forward (not dispositions)**
- C308: `signal_engine._kelly` applies a third fee form (`b·(1−0.07)` odds haircut) — neither the curve nor the flat 0.0175; flag for the §3 (sizing) verifier.
- C321: `analyze_late_window.TWAP_SWITCH_TS` = 08-07 vs `analyze_twap_lock.TWAP_SWITCH_TS` = 08-14.
- C330: the RTDS subscription list has four topics; §9 names three (the fourth is record-only).
- C339: 02_data_facts attributes the 08-27 06:45Z restart to `needrestart` after an unattended apt upgrade inside trading hours — a restart path §10 does not describe.
