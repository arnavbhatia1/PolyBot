# 03 — Verification C198–C290 (§4 Orders, §5 Resolution, §6 Recorders + nightly)

Verified 2026-08-28 against branch `main` (HEAD `15471a9a`; code cited as `path:line`, roots
`polybot/` unless `scripts/` is given), `docs/audit/data/` (DB backups 20:27Z 08-27, `polybot.log`
08-26 13:31→08-27 20:26 + `polybot.log.1` 07-13→08-26, state JSON), the git history of the
nightly-committed `polybot/memory/state/latency_stats.json` (24 versions 07-05→08-14, treated as
data), `scripts/research/data/vps-0821/` (tape/micro 08-14→08-27, `window_paths_60s.db`,
`latency_report.md`, `r5_report.md`), and `docs/audit/02_data_facts.md`. Phase 1 surveys
(`01a/01b/01c/01d`) were spot-checked before any of their line refs were reused. Docs
(CLAUDE/RESEARCH/REFUTATIONS/README, docstrings, code comments) carry zero weight — a claim
backed only by them is UNVERIFIED.

Log counts below are over both log copies (07-13→08-27) unless dated.

| id | disposition | evidence | note |
|---|---|---|---|
| C198 | VERIFIED-CODE | `execution/live_trader.py:13-22` imports `py_clob_client_v2` (`ClobClient`, `OrderType`, `BUY/SELL`); `:1810-1827` `create_market_order` + `post_order(signed, OrderType.FOK)`; installed 1.0.0 (`pip show`) | Paper never touches the client — `paper_trader.py:333-402` walks the WS book. |
| C199 | VERIFIED-CODE | `requirements.txt:14` `py_clob_client_v2>=1.0.0,<1.1.0`; local venv resolves 1.0.0 | — |
| C200 | UNVERIFIED | Only the `requirements.txt:10-13` comment states it; no 1.1.0 source is in the audit set. `r5_report.md` row "CLOB order types" documents the exchange-side Jul-24 tradeID change, not the client's poll | External library behaviour, not checkable locally. |
| C201 | VERIFIED-CODE | `live_trader.py:58 _MAX_RETRIES = 3`; `:1792 for attempt in range(1, _MAX_RETRIES + 1)`; `:2003` exhausted reason | Paper has no retry (`paper_trader.py:246-257`). |
| C202 | VERIFIED-CODE | `live_trader.py:122-128 _exchange_rejected` (HTTP 4xx = evaluated and killed → no retry `:1950-1962`); `:62-66 _NON_RETRYABLE_ERRORS` → unfilled `:1835-1838`; ambiguous POST → WS-VWAP check, then "not retried" `:1963-1983`; unmatched → cancel-and-settle `:1938-1940, 562-630`; only `success=False` non-fatal responses and pre-POST exceptions loop `:1839-1843, 1984-1993` | Nothing after `status == "matched"` re-enters the loop. |
| C203 | CONTRADICTED | `_LATENCY_QUANTILES` p25/p50/p75 = 0.410/0.436/0.679 (`paper_trader.py:288-291`) equal the ledger committed 2026-07-08 (`git 293cbae5` latency_stats.json: n=20, p25 410.1 / p50 436.1 / p75 679.2). The 19 daily ledgers after it (07-20→08-08, n=8..74) read p50 312-432 (median ≈348); the last (08-13, n=2) reads 302.9 | "Last measured" is 07-08, not pre-08-13; every later ledger except 08-01 (n=74, 432.3) sits below the table. The ops watch reads the current file as dark (n=2 < 10). |
| C204 | VERIFIED-DATA | `r5_report.md:13` (read 08-27): changelog "taker delay on crypto markets is now 50ms, down from 250ms … August 17 at 11:00 UTC"; the table is the 07-08 ledger (C203, n=20) measured under the 250 ms hold; `paper_trader.py:293-296` names the hold | The literal `itode: true` flag appears in no local artifact (`r5_clob_market.json` shows only `seconds_delay: 0`). |
| C205 | VERIFIED-DATA | `scripts/research/data/vps-0821/r5_report.md:13` quotes the docs.polymarket.com changelog entry "Aug 17, 2026" verbatim; raw HTML grep confirmed 08-27 (`r5_changelog.html`) | Single primary source — r5 found no X/blog post for it. |
| C206 | VERIFIED-DATA | Table = 07-08 ledger (C203); hold cut 08-17 (C205); zero POST samples since 08-13 (`latency_stats.json last_updated 2026-08-13`, n=2) | Staleness is inferred from the route change; no post-08-17 sample exists to measure it. |
| C207 | CONTRADICTED | `scripts/smoke_order_test.py:115` posts via the raw client — `client.post_order(client.create_market_order(mo), OrderType.FOK)` — bypassing `LiveTrader._record_submit_latency`; it prints one "Warm-POST RTT sample" and writes nothing. No code re-derives `_LATENCY_QUANTILES`; it is a literal (`paper_trader.py:288-291`) | The ledger fills only from live FOK fires (`live_trader.py:1808, 1827, 1957`) — a path `taker_enabled: false` keeps dormant. |
| C208 | VERIFIED-DATA | `latency_stats.json` @ `git 69add48b` (2026-07-21): sign n=17, p50 17.5 ms — pure-python era (coincurve entered `requirements.txt` in d470a766, 07-23). From 07-24 the box signs at 1.5-5.6 ms (n=1..9/day, 8 ledgers 07-24→08-13); current file 4.3 ms, n=1 | Real but historical: the box has run coincurve since 07-24 (`latency_report.md` §3: coincurve 21.0.0 importable 08-21). |
| C209 | UNVERIFIED | Ledger pairs bracket it: pure-python 17.5/11.5/11.1 ms (n=17/10/9, 07-21..23) vs coincurve 1.5-5.6 ms (n=1..9, 07-24→08-13) → 3×-12×, pooled ≈4.5× | "~10×" is the top of the range, not the central estimate; only the `requirements.txt:15-16` comment says 10×. |
| C210 | VERIFIED-CODE | `requirements.txt:18` `coincurve>=19.0; sys_platform == "linux"` (uvloop likewise `:21`) | — |
| C211 | CONTRADICTED | `warm_sell_signature` (`live_trader.py:1091`, `paper_trader.py:107`) has no caller in `polybot/` or `scripts/` (grep: definitions + one debug line); its docstring's "main.py calls this on HOLD ticks" has no counterpart in `main.py`. `_take_sell_warmup` is reached only on the SELL branch `:1788`, i.e. via `close_trade`, which has zero production callers (01b §6.4) | Dead code — nothing is pre-armed. |
| C212 | VERIFIED-CODE | `main.py:1305-1308` spawns `warm_buy_signature` after the last veto gate, then awaits `open_trade`; `live_trader.py:1789-1791` `_await_buy_warmup_inflight` + `_take_buy_warmup` consume it | Live-only (`hasattr` false in paper) and on the taker path, which `taker_enabled: false` neutralises — never runs in the checked-in config. |
| C213 | VERIFIED-CODE | `live_trader.py:1760-1769` pre-check reads `self._clob_ws.get_book` only, book ≤5 s, no REST fallback; `main.py:1277-1282` pre-submit VWAP walk uses `clob_ws.get_book` ≤10 s only; paper `paper_trader.py:195-244` same | The upstream gate books (not the pre-check) do fall back to REST `fetch_clob_book` when WS is stale (`main.py:1466-1475`). |
| C214 | VERIFIED-CODE | `live_trader.py:33-41` replaces the py-clob HTTP client with one `httpx.Client(http2=True, max_keepalive_connections=5, keepalive_expiry=60)`; `prewarm_http :411-423`; `start_keepalive :442-460` pings every 5 s; wired `main.py:2593-2594` | `requirements.txt:1 httpx[http2]`. |
| C215 | VERIFIED-CODE | `main.py:2989-2992` `gc.collect(); gc.freeze(); gc.set_threshold(10_000, 20, 20)` after the Discord wait, before `trading_loop` is created | — |
| C216 | VERIFIED-CODE | `execution/maker_bid.py:83-99`: `snapped = round(int(round(px/tick, 6))*tick, 10)` (`int()` truncates → down for px>0); `max(tick, min(snapped, 1−tick))`; applied per rung `:153` | Tick from `market_scanner.fetch_tick_size` (1 h cache, "0.01" on error). |
| C217 | VERIFIED-CODE | `maker_bid.py:44 MIN_SHARES = 5.0`; `:155-162` a rung with `shares < MIN_SHARES` is skipped, never posted | That 5 is the exchange minimum rests on the code comment only; no local artifact states the exchange rule. |
| C218 | VERIFIED-CODE | `live_trader.py:658-660` (no order id in response) and `:665-667` (exception) both `logger.error("MAKER BID REJECTED …")`; `maker_bid.py:173-174` relies on it | Data: 0 occurrences 07-13→08-27 (live GTC ran only 08-14/15). |
| C219 | VERIFIED-CODE | `maker_bid.py:159-161` `logger.info("MAKER RUNG SKIPPED …")` | Data: 1,355 in `polybot.log.1` (07-13→08-26), 0 in `polybot.log` (08-26→27); concentrated 08-22..24. |
| C220 | VERIFIED-CODE | `main.py:1236-1256` stamps `cl_report_to_submit_ms`, `lat_wake_to_eval_ms`, `lat_fast_path`, `lat_sig_woke`, `lat_clob_feed_ms`, `lat_segs_ms{sched,gate,disc,books,bkgates,px,pos,tick}`, `lat_ctx_ms` into `trade_context` | Taker path only: deep_proj ladder fills carry none (`maker_bid.py` snapshot; `latency_report.md` §0). "Per fill" holds for lock_dip fills — none since 08-13. |
| C221 | VERIFIED-CODE | `maker_bid.py:163-172` `place_ms` per rung; `:306-309` `cancel_ms`; `:346-347` lists into `trade_context.gtc_place_ms/gtc_cancel_ms` | Data: 19/38 paper positions carry `gtc_place_ms` (rows before the stamp shipped lack it). |
| C222 | VERIFIED-CODE | `live_trader.py:178-205 _record_gtc_latency` writes `stats["gtc"][kind] = {n, p50_ms, p90_ms, max_ms, samples_ms}`, merge-preserving with the FOK writer `:208-248` | Data: no `gtc` key in any committed `latency_stats.json` (24 git versions 07-05→08-14) nor the 08-27 copy — never populated. |
| C223 | VERIFIED-CODE | `scripts/smoke_gtc_test.py:123,165` call `trader.place_gtc_bid` (LiveTrader → `_record_gtc_latency`); `:85-88 --samples`; `:191` "samples persisted to latency_stats.json (gtc section)" | Never run with samples (C222 data). |
| C224 | VERIFIED-CODE | `main.py:528 _OWNED_LAT_BUDGET_MS = 25.0`; `:1340-1355` `if _owned_ms > _OWNED_LAT_BUDGET_MS * 1.5: … logger.warning("LATENCY BUDGET …")` | Data: 0 occurrences (shipped 08-22; no stamped taker fill since). |
| C225 | VERIFIED-CODE | `main.py:2470 verify_auth(min_allowance_usd=…)` → `live_trader.py:316-328` `_create_clob_client` (raises on missing key/funder env `:263-265`); boot aborts on `not ok` `main.py:2471-2473` | — |
| C226 | VERIFIED-CODE | `main.py:2463-2473` `_min_allowance = bankroll × kelly_fraction × max_concurrent × 10`; `verify_auth :330-348` fetches balance + allowance and fails below the floor | — |
| C227 | VERIFIED-CODE | `live_trader.py:67 _ALLOWANCE_RECHECK_EVERY = 10`; `:478-483` counter; sole call site `:1914`, on the matched-fill path (fire-and-forget) | Docstring says "submits"; the call site makes it fills. |
| C228 | VERIFIED-CODE | Live BUY `live_trader.py:1924-1928 fill_size=notional_usdc` (= `amount`, the requested USDC); `:606, :618, :1974` also USDC; SELL `amount × fill_price`; paper `paper_trader.py:342 size_usd`, `:402 spent` | `open_trade` derives shares as `fill_size / fill_price` (`base.py:334`) — consistent. |
| C229 | VERIFIED-CODE | `main.py:1688-1707`: `event_metadata.final_price >= price_to_beat` decides; a CLOB disagreement only logs `:1696-1703` | Fallback 2 (`:1710-1715`) lets a coherent closed book decide when metadata is absent — a non-oracle path, by design. |
| C230 | VERIFIED-CODE | `db/models.py:276-319 close_position`: history INSERT + positions UPDATE + bankroll write under `_write_lock`, one `commit()`, `rollback()` on any `BaseException` | Live "credit" is the wallet read after auto-redeem (`live_trader.py:685-758`); atomicity is the DB booking. |
| C231 | VERIFIED-CODE | `main.py:1688-1691` priority 1 = `live["event_metadata"]` `final_price`/`price_to_beat`, parsed from Gamma `eventMetadata` at `feeds/market_scanner.py:154-164` | — |
| C232 | VERIFIED-CODE | `main.py:1708-1715`: `closed` and `0.98 <= up+down <= 1.02` and `price_up >= 0.99 or <= 0.01` | — |
| C233 | VERIFIED-CODE | No Binance reference in `_resolved_exit_price` / `_resolve_expired_position` / `_manage_orphaned_position` (`main.py:1673-1956`); Binance enters the codebase only via `chainlink_feed.spot_bridge_delta` | — |
| C234 | VERIFIED-CODE | `main.py:1859-1882`: both `get_strike(w)` and `get_strike(w+300)` are taken only if `strike_reliable`; either missing → "Waiting for resolution (boundary captures incomplete)" | Excludes `get_strike`'s `twap_official` fallback (`feeds/chainlink_feed.py:145-148`). |
| C235 | VERIFIED-CODE | `main.py:1878-1882` waits; `:1894-1901` Discord `send_error` on a fallback resolve; `:1905-1911` ERROR + `send_trade_closed("orphaned — awaiting resolution")` past 3600 s | Data: 0 `ORPHAN` lines 07-13→08-27. |
| C236 | VERIFIED-CODE | `main.py:1750-1758`: while `exit_price is None` and both trusted captures exist, one `TAPE VERDICT` line | Data: 379 lines (377 + 2), 08-07 04:35Z → 08-27 05:46Z. "Before Gamma serves" holds by construction (logged only while unresolved); the lead time is not measured. |
| C237 | VERIFIED-CODE | `main.py:1768-1775` `logger.warning("RESOLUTION DRIFT …")` when `abs(tape_final − Gamma final) > 0.005`, once per market | Data: 6 warnings — 1 on 08-07 11:56Z (30s cutover day), 5 on 08-14 22:30Z→08-16 17:10Z (bot still on the 30s stream); 0 since the 08-18 migration. |
| C238 | VERIFIED-CODE | `main.py:2799-2801` `mechanism_read(chainlink_feed.boundary_snapshot(), …)`; `_mech_line :2896-2919` | Coverage caveat: `boundary_snapshot` holds only trusted captures from the last 7200 s (`chainlink_feed.py:318-325, 429-435`), so the nightly compares ~2 h — 41/41, 43/43, 45/45 windows in the 08-25/26/27 pings vs 288 labels/day. The every-window net is the per-window gate (C268). |
| C239 | VERIFIED-CODE | `live_trader.py:685-758`: wins (`exit >= 0.99`) stay pending until `_chain_token_shares(winning token) <= dust`, then the wallet balance is booked; no redeem call anywhere (grep) | `PAYOUT STUCK` CRITICAL at 600 s; data: 1 "REDEEM STUCK" 07-30. |
| C240 | VERIFIED-CODE | No redeem/merge/`ConditionalTokens`/web3 call in `polybot/` or `scripts/` (grep); `reconcile_dust` logs `UNREDEEMED WINNINGS` CRITICAL for winners and lists losers without action (`live_trader.py:1447-1462`) | "Deliberately" rests on the comment `:1450-1451`; the absence of automation is what the code shows. |
| C241 | VERIFIED-CODE | The only signing calls are order construction `create_order`/`create_market_order` (`live_trader.py:646, 1810`) and API-cred derivation `derive_api_key`/`create_api_key` `:282-284`; no `web3`, `eth_account`, `send_raw_transaction`, `approve` (grep) | API-key derivation is an off-chain EIP-712 auth message — not on-chain, so the claim holds as worded. |
| C242 | VERIFIED-CODE | `recording.py:493-494` `await asyncio.sleep(0.2 if late else 1.0)` | Data: `window_paths_60s.db` 08-14→08-21, n=2,088 windows: 250.6 rows/window at `elapsed_s < 255` (255 expected). |
| C243 | VERIFIED-CODE | `recording.py:493` `late = 255 <= elapsed <= 300` → 0.2 s sleep | Data: 220.4 rows/window at `elapsed_s ≥ 255` (225 expected), same n=2,088 windows. |
| C244 | VERIFIED-CODE | `recording.py:107-117, 150-175` columns `bid/ask_up/down`, `depth3_*`, `bid_sz/ask_sz_*`, `chainlink_price`, `chainlink_age_s`, `strike`, `strike_trusted`, `traded` | `coinbase_*`, `binance_*`, `atr`, `model_prob_up`, `depth20_*` record NULL (deleted feeds) — 01c §3.1. |
| C245 | VERIFIED-CODE | `recording.py:387-389` `strike_trusted = 1/0` via `strike_reliable`; `chainlink_feed.py:139-149 get_strike` returns any captured value plus a `twap_official` fallback regardless of trust | Data: the 08-21 snapshot predates the column (33 cols, no `strike_trusted`); no later `window_paths.db` is in the audit set. |
| C246 | VERIFIED-CODE | `recording.py:466-494` runs unconditionally with time-based discovery every window (`int(time.time()//300)*300`); started in both modes | Data 08-14→08-21: 2,088 of 2,216 slots; the 128 missing = 3 single windows at 04:05/04:10Z (the midnight-ET restart slot) + 125 consecutive 08-17 04:10→14:30Z (the 08-17 outage). Every window while the process runs. |
| C247 | VERIFIED-CODE | `recording.py:30 PATHS_DB = polybot/db/window_paths.db`; `.gitignore` `polybot/db/window_paths.db*`; labels live in the per-mode DB `recording.py:124-140` | Labels therefore accrue only in the active mode's DB. |
| C248 | VERIFIED-CODE | `recording.py:792-797 cleanup_job(db, retention_days=90)` deletes `ts < now − 90 d`; registered with defaults `main.py:2708` | Untestable on data (snapshot spans 8 days). |
| C249 | CONTRADICTED | The bar / kill-rule read `scripts/analyze_late_window.py:91-105 live_health_read` selects `FROM trade_history t JOIN positions p …` — `window_labels` is not in the query; `kill_rule_tripped` derives from `t.pnl` (`:146-177`). `window_labels` feeds `mechanism_read :224`, `queue_depth_read :275`, `resolution_snapshot_read :351` and the SIM ceiling, which "never feeds the verdict" (`main.py:2822-2825`) | Labels are the watch/research ground truth; the binding bar's ground truth is realized `trade_history`. |
| C250 | VERIFIED-CODE | `recording.py:518-531` fields `ts, token, price, size, side, ets (exchange_ts), fee_bps`; `:551` `tape_{day}.jsonl` under `memory/recordings/`; wired via `_on_trade_mux` `main.py:2661-2665` | Data: `tape_2026-08-26` holds 399,980 prints (01c §6.1). |
| C251 | VERIFIED-CODE | `.gitignore` `polybot/memory/recordings/` | Local dir holds 08-07→08-27 files, none tracked. |
| C252 | VERIFIED-CODE | `recording.py:573 _LATE_ELAPSED_S = 210.0`; `:585-586 _late`; `:591-592` `on_bba` returns unless late → "b" rows only at elapsed ≥ 210 s | Data: b = 10,540,293 of 10,860,043 lines in `micro_2026-08-26` (01c §3.3). |
| C253 | VERIFIED-CODE | `recording.py:656-672` `on_cl_report` → `{"k":"l", ts (payload), rx, p, pub}`; wired `main.py:2668` | Data: l = 78,896 on 08-26. |
| C254 | VERIFIED-CODE | `recording.py:602-622` `{"k":"t", ts, rx, p, pub}` from `on_twap`; wired `main.py:2669` | Data: t = 78,908 on 08-26. |
| C255 | VERIFIED-CODE | `chainlink_feed.py:604-611`: `crypto_prices_twap_thirty` → `on_twap30` hook only, `continue` before any state ("recorded only — never touches price/strike state"); `recording.py:624-639` writes "t3" | "Retired" = as resolution source (r5: changelog Aug 14); the topic itself is served again (C257). |
| C256 | UNVERIFIED | Intent only: comments at `main.py:2904-2905` and the `recording.py` docstring. No reader consumes "t3" — `scripts/research/ws1_reduce.py:89` explicitly skips it; no script compares t vs t3 | Evidence-in-waiting; nothing today performs the A/B. |
| C257 | VERIFIED-DATA | Nightly pings: t3 = 42,683 (08-25 04:02Z), 30,583 (08-26 04:04Z), 78,879 (08-27 04:03Z); `micro_2026-08-26.jsonl.gz` t3 = 78,929 (01c); `r5_report.md:15` live listen 08-27 18:38Z: 20 `crypto_prices_twap_thirty` msgs in 20 s | Served intermittently from at least 08-24 (~12 h and ~8.5 h of ~1 Hz), full-day by 08-26. |
| C258 | VERIFIED-CODE | `main.py:2801` passes `micro_tape.t3_records`; `:2906-2910` appends "30s A/B tape N records" or "… EMPTY" | Data: the 08-25/26/27 ping lines carry the count. |
| C259 | VERIFIED-CODE | `recording.py:641-654` `{"k":"s", "src":"bz", ts, rx, p}` from `on_spot`; wired `main.py:2671` | Data: s = 83,017 on 08-26. |
| C260 | VERIFIED-CODE | l/t/t3/s rows carry payload `ts` + receipt `rx` (`recording.py:617, 633, 649, 667`); file `micro_{day}.jsonl` `:692` | "b" rows carry receipt `ts` only (BBA messages have no exchange ts). |
| C261 | VERIFIED-DATA | Same-content measurement: 209,715,132 B of `micro_2026-08-21.jsonl` → 4,282,026 B at gzip level 3 (the job's default, `recording.py:698`) = 49×; full-day estimate for 08-26: 10,860,043 lines × 150.2 B/line ≈ 1.63 GB vs 42,248,997 B gz ≈ 39× | Tape compresses only ~14× (52.4 MB → 3.7 MB sample). The on-disk raw/gz pairs (08-18, 08-21) are partial-day pulls and read 27-29× — not same-content, do not cite them. |
| C262 | VERIFIED-CODE | `scripts/analyze_twap_lock.py:123-135` tries `.jsonl` then `.jsonl.gz`, `gzip.open` for `.gz`; `scripts/analyze_late_window.py:298` same in `queue_depth_read` | — |
| C263 | VERIFIED-CODE | Fills: taker snapshot `main.py:1223-1272`, ladder snapshot `main.py:1083-1096`, `book_maker_fill` stamps `maker_fill` `execution/base.py:416-419`; ghosts: `_ghost` merges `trade_context` into `indicator_snapshot` `main.py:939-966` | Ghosts need a downstream veto of a BUY; with `taker_enabled: false` gate 8 remaps to SKIP first, so the current config produces none (08-27 nightly: "0 ghosts"; local `ghost_outcomes/` absent). |
| C264 | VERIFIED-CODE | `scripts/analyze_late_window.py:120-124` `leg = trade_context.signal_leg or "unstamped"`, per-leg buckets; stamped `main.py:1084` (`deep_proj`), `:1099` (`lock_dip`) | Data: paper 38/38 `deep_proj`; live 60s-era 5/5. |
| C265 | VERIFIED-CODE | `main.py:1236-1256`: `cl_report_to_submit_ms … if _eval_cl_rx > 0 else None`, `lat_wake_to_eval_ms … else None`, segs omitted when marks are missing, `lat_ctx_ms … else None`; `:1090-1091 twap_proj_plain … else None`; `_ghost :949-950` | `latency_report.md` §2 confirms None rows among the 36 stamped fills (e.g. `lat_clob_feed_ms` None in 13/36). |
| C266 | VERIFIED-CODE | `feeds/clob_ws.py:211` `last_print_gap_ts = time.time()` on any drop; `maker_bid.py:340-344` `snapshot["print_gap"] = int(gap_ts >= placed)` (None if the feed never dropped) | Data: paper positions `print_gap` = 0 on 19, absent on 19 (pre-stamp rows), never 1; live 60s-era 5 absent. Written, never read (01c §5). |
| C267 | VERIFIED-CODE | Paper fills come only from `on_print` (`maker_bid.py:191-218`); prints during a drop are never delivered (`clob_ws.py:212-213` "prints are lost until it reconnects"; `:183` clears buffers on reconnect) | Mechanism holds; magnitude unmeasured — no `print_gap = 1` fill exists to quantify it. |
| C268 | VERIFIED-CODE | `recording.py:325-360 _check_resolution_source`, called after every successful label `:323` | — |
| C269 | VERIFIED-CODE | `recording.py:337 caps = chainlink_feed.boundary_snapshot()` (trusted only, `chainlink_feed.py:318-325`); compares `("strike", ptb, ep)` and `("final", fp, ep+300)` `:345-348` | Labeled windows only: a window Gamma never labels (dropped after 2400 s) is never compared and does not bump `source_unchecked` (`:356-360` counts labeled-but-uncaptured only). Data: 32 `SOURCE CHECK SKIPPED`, 0 fires. |
| C270 | VERIFIED-CODE | `recording.py:350 if abs(served - cap) > 0.005:` → `on_source_mismatch` → `main.py:2685 config.get("late_window", {})["trading_enabled"] = False`; the loop re-reads it every iteration `main.py:2212-2213` | Data: 0 `RESOLUTION SOURCE MISMATCH` 07-13→08-27 (gate shipped 08-18). |
| C271 | VERIFIED-CODE | `main.py:2691-2697` `alert_manager.send_health("🚨 TRADING HALTED — resolution source mismatch …")` → `#polybot-daily` (`discord_bot/alerts.py:118-136`) | Plus CRITICAL log `:2686-2690`. |
| C272 | VERIFIED-CODE | `main.py:2685` mutates the in-memory dict only; no `yaml.dump` / settings write anywhere in `polybot/` (grep) | — |
| C273 | VERIFIED-CODE | Latch `recording.py:97, 351 _source_mismatch_fired` never resets; the config flip lives in process memory → only a restart (re-reading `settings.yaml`) re-arms | "After re-pointing the feed" is operator instruction (comment `main.py:2683-2685`), not enforced. |
| C274 | VERIFIED-CODE | Repo-wide grep for runtime `config[...] = ` assignments: exactly one hit, `main.py:2685` | Discord `!pause` uses its own flag, not config. |
| C275 | VERIFIED-CODE | `config/settings.yaml:198-199` `daily_pipeline_hour: 23 / daily_pipeline_minute: 45`; `agents/scheduler.py:84-89` `ZoneInfo("America/New_York")`, fires when `hour == 23 and 45 <= minute < 50` | Data: pipeline start 03:45:52Z on 08-27 (= 23:45 EDT). |
| C276 | VERIFIED-CODE | `scheduler.py:52-53` rollups; jobs `main.py:2707-2731, 2965`: compress, window_paths_retention, price_sum_retention, maker_ladder, recordings_retention, sniper_health | `maker_ladder` (report-only) and `price_sum_retention` also run, unlisted in the claim. |
| C277 | VERIFIED-CODE | `main.py:2943-2945 alert_manager.send_health(msg)`; `alerts.py:118-136` → `daily_channel_name`; `settings.yaml:216 polybot-daily` | Data: 5 `NIGHTLY PING` emissions 08-21→08-27, 0 `NIGHTLY PING LOST`; Discord delivery itself is not observable locally. |
| C278 | VERIFIED-CODE | `main.py:2773-2782 live_health_read(…, validation_epoch)`; `:2822-2827` `kt` from `live["kill_rule_tripped"]` only ("The SIM never feeds the verdict"); `_legs_line :2861-2866` | The displayed "Shut-off line" (+2.0¢ / ≥ 2.0, `:2838-2846`) is text, not the computed rule (trailing-4-day $ mean < 0 with ≥ 4 days / ≥ 5 fills, `analyze_late_window.py:146-177`). |
| C279 | VERIFIED-CODE | `main.py:2762-2763 health_read` under a 240 s cap; `_context_line :2848-2859` "Research sim (a CEILING …)" | Data: 08-27 "SIM read skipped (TimeoutError)" — the line is omitted when the read fails. |
| C280 | VERIFIED-CODE | `analyze_late_window.py:390-397` `gap_p25/p50/p75`, `photo_finish_pct` = share `< 1.0`, needs ≥ 24 gaps; `main.py:2868-2882 _regime_line` | Trailing 26 h of labels (`hours=26.0`). |
| C281 | VERIFIED-CODE | `main.py:2875-2876` `if r["gap_p50"] < 6 or r["photo_finish_pct"] > 15` → HOSTILE | — |
| C282 | VERIFIED-CODE | `git a92b4f43` (2026-08-18 14:53 -0400, "WS2: regime stack recalibrated for the 60s rule"): `- gap_p50 < 8` → `+ gap_p50 < 6` in `main.py`; photo band $2 → $1 in `analyze_late_window.py`; introduced by 14bcf9a2 (08-15) at `< 8` | Git history is the evidence; the "percentile-ported" derivation itself is a comment. |
| C283 | UNVERIFIED | Paper DB 08-18→08-27 (38 fills, 10 ET days): every fill day is PAYING by the code's thresholds (gap p50 $20-$103, photo 0.3-1.7%); the only HOSTILE day (08-15: p50 $5.1, photo 17.4%) precedes the 08-17 paper reset, so its 0 fills are an artifact | Untestable on the audit set; stated only in the `main.py:2870-2874` comment. (The two loss days, 08-20/21, were the widest-gap days — n=2.) |
| C284 | VERIFIED-CODE | `main.py:2790 resolution_snapshot_read`; `analyze_late_window.py:374-385` `abs(final − next.price_to_beat) < 0.005`; `_twap_line :2884-2895` | Data: "311/311" in the 08-27 ping. |
| C285 | VERIFIED-CODE | `main.py:2799-2801` → `analyze_late_window.mechanism_read :198-251`; `_mech_line :2896-2919` | Data: 41/41, 43/43, 45/45 bit-exact (08-25/26/27). Coverage caveat as C238. |
| C286 | VERIFIED-CODE | `main.py:544-548` `drift = (p50 − 436)/436`, flagged `> 0.25`; `_latency_watch :461-478` dark when `n < 10` or age > 7 d | Baseline 436 is the 07-08 table (C203) measured under the 250 ms hold; dark today ("only 2 order samples"). |
| C287 | VERIFIED-CODE | `main.py:2808-2809 queue_depth_read(7.0, …)` under a 120 s cap; `analyze_late_window.py:254-330` (≥ 50 sweeps; at-level volume before a strictly-lower print within 60 s); `main.py:552-558` p75 vs 135 ±25% | Data: 08-27 "queue depth read skipped" (timeout) — the line was absent that night. |
| C288 | VERIFIED-CODE | `main.py:502-522 _gtc_watch` dark when `gtc.place.n < 10` or age > 14 d; `:560-570` p50 vs 56 ±25% or KS > 0.30 (`_gtc_table_ks :481-499` against `_GTC_LATENCY_QUANTILES`) | Data: dark on every ping ("GTC RTT unmeasured — only 0 GTC samples"). |
| C289 | VERIFIED-CODE | `main.py:528-529` counter, `:1345-1347` increment, `:573-575` "N fills over the 25ms owned-latency budget today" | Data: 0 breaches; the counter is a module global, reset per process. |
| C290 | VERIFIED-CODE | `_sniper_health_job` (`main.py:2731-2964`) assigns nothing to `config` and writes no file; the only runtime config write in the codebase is the per-window SOURCE gate `main.py:2685` (not a nightly job); the nightly's writes are retention deletes, rollups and gzip (01c §4.6) | `analyze_late_window.py:84` docstring agrees ("the caller never flips config"). |

## Summary

| disposition | count | ids |
|---|---|---|
| VERIFIED-CODE | 79 | C198, C199, C201, C202, C210, C212–C228, C229–C248, C250–C255, C258–C260, C262–C282, C284–C290 |
| VERIFIED-DATA | 6 | C204, C205, C206, C208, C257, C261 |
| CONTRADICTED | 4 | C203, C207, C211, C249 |
| UNVERIFIED | 4 | C200, C209, C256, C283 |
| EXPIRED | 0 | — |

93 rows, C198–C290, each exactly once.

**CONTRADICTED**
- **C203** — the 410-436 ms table is the 2026-07-08 ledger (n=20); "last measured" ledgers 07-20→08-13 read p50 312-432 (median ≈348), final 302.9 (n=2).
- **C207** — `smoke_order_test.py` posts through the raw client and never writes `latency_stats.json`; no code re-derives `_LATENCY_QUANTILES`, which is a hand-typed literal.
- **C211** — `warm_sell_signature` has no caller; the only SELL consumer sits behind `close_trade`, which has zero production callers. No SELL signature is pre-armed.
- **C249** — the kill-rule / bar read queries `trade_history JOIN positions`; `window_labels` never enters it (labels feed the watches and the SIM, which never feeds the verdict).

**EXPIRED** — none in this range.

**Noteworthy caveats on VERIFIED rows (for the CLAUDE.md rewrite)**
- C208/C209: 17.5 ms is the 07-21 pure-python measurement (n=17); the box has signed with coincurve at 1.5-5.6 ms since 07-24. The speed-up brackets 3-12×, pooled ≈4.5×.
- C238/C285: the nightly SOURCE watch covers only the trailing ~2 h (`boundary_snapshot` prunes at 7200 s → 41-45 windows/night vs 288 labels/day). The per-window gate (C268-C270) is the every-window net.
- C212/C220/C224: BUY pre-sign, `lat_*` stamps and the LATENCY BUDGET check live on the taker path, which `taker_enabled: false` neutralises — none has executed since 08-13.
- C222/C223/C288: the `gtc` section has never been populated (24 committed ledger versions, 0 with `gtc`); the GTC watch has been dark on every ping.
- C246: 2,088 / 2,216 windows recorded 08-14→08-21; the gaps are the midnight-ET restart slot (3) and the 08-17 outage (125).
- C261: "~39×" holds for full-day micro-tape (49× on a same-content sample; ≈39× full-day estimate); tape gzips ~14×.
- C263: the current config produces no ghosts at all (taker remapped to SKIP before any ghost gate).
- C269: unlabeled windows are never source-checked and do not increment `source_unchecked`.
- C278: the ping's "Shut-off line" thresholds (+2.0¢ / t ≥ 2.0) are display text, not the computed kill rule.
