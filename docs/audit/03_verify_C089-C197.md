# 03 — Claim verification C089–C197 (§2 The two legs, §3 Sizing)

Verified 2026-08-28 against `main` (HEAD 03349951 + 4fc8f847 merge), read-only. Ground truth in order: code
`[path:line]`, data (`docs/audit/data/`, `scripts/research/data/vps-0821/` incl. experiment reports), tests,
`settings.yaml`, then docs (zero weight). Abbreviations: `se` = polybot/core/signal_engine.py, `mb` =
polybot/execution/maker_bid.py, `main` = polybot/main.py, `cl` = polybot/feeds/chainlink_feed.py, `pt` =
polybot/execution/paper_trader.py, `lt` = polybot/execution/live_trader.py, `base` = polybot/execution/base.py,
`cb` = polybot/execution/circuit_breaker.py, `rec` = polybot/recording.py, `alw` = scripts/analyze_late_window.py,
`settings` = polybot/config/settings.yaml, `r1` = scripts/research/data/vps-0821/r1_report.md (+ r1_tables.json,
r1_regime.json, r1_*.log), `r23` = .../r23_report.md, `ws1_*`/`ws2_*`/`ws3_*`/`ws4_*` = scripts/research/*.py,
`tmb` = polybot/tests/test_maker_bid.py, `tlh` = polybot/tests/test_live_health_read.py, `tls` =
polybot/tests/test_late_sniper.py, `tcb` = polybot/tests/test_circuit_breaker.py.

Policy/prohibition rows: VERIFIED-CODE means the rule is restated in code or a config comment (cited); the note
says whether anything enforces it. Pointer rows ("X is in RESEARCH.md") verify only that the text exists.

| id | disposition | evidence | note |
|---|---|---|---|
| C089 | VERIFIED-CODE | `se:34-45` `TWAP_MARGIN_P995` / `TWAP_MARGIN_MAX` tuples; consumed at `se:118,121`, `main:1062`, `mb:277` | 16 knots each, k=2..58. |
| C090 | VERIFIED-CODE | `se:16` header "re-fit 2026-08-27"; git 03349951 2026-08-27 "Margin tables re-fit on 14 real-final days"; r1 dated 2026-08-27 | Tuples in `se:34-45` are identical to `r1_tables.json.literals`. |
| C091 | VERIFIED-DATA | `r1_tables.json` corpus.real_final_windows = 3695; r1 "Corpus": 3,787 real-final windows in win_streams, 92 with no gated knot sample; 08-13..27 ET | 3,695 = windows with at least one veto-passing, coverage-gated sample. |
| C092 | VERIFIED-DATA | `r1_tables.json` corpus.et_days = 15 entries 08-13..08-27; per-day counts 46..284 | Only 13 are full days: 08-13 has 46 windows (era starts 20:00 ET), 08-27 has 168 (labels end 18:10 UTC). |
| C093 | VERIFIED-DATA | `r1_freeze_tables.log:2` "3695 real-final windows + 1651 synthetic (max-union only)"; `r1_measure60.log:2` 1,708 synthetic before the veto/gap filter | 1,651 is the post-filter count. |
| C094 | VERIFIED-CODE | `ws1_freeze_tables.py:44-46` p995 fitted on `real` rows only; `:53-62` MAX taken from `interval_max_knots()` (real + synthetic union) | Confirmed in `r1_tables.json`: `interval_max_real == interval_max_all` at all 16 knots. |
| C095 | VERIFIED-CODE | `ws1_measure60.py:4-5,133-135`: target = served final if `ep >= RULE_TS` else `a60rx(close)` (own rx-clock 60s running average) on pre-rule tape | RULE_TS = 1786665600 [`ws1_measure60.py:23`]. |
| C096 | VERIFIED-CODE | `ws1_measure60.py:3,133-135,222-235`: the synthetic target and the projection's A-term are the same `running_avg`; validation on real days a60rx-vs-served med $0.013-0.08 | Follows from construction (target = estimator as k goes to 0); not separately measured as a bias number. |
| C097 | VERIFIED-CODE | `ws1_freeze_tables.py:44-62`: P995 from real only; MAX = max over union, which is at least the max over real by construction; `r1_tables.json` interval_max_real == interval_max_all (16/16) | r1: "it widened nothing" this fit. |
| C098 | VERIFIED-CODE | `ws1_measure60.py` rx-clock `running_avg`; gap <= 10 filter `ws1_freeze_tables.py:27`; `ws1_interval_max.py:24,56-59` RAW_GAP_MAX 10; engine guard `cl:246-257` | `se:24-26` states the guard is part of the measurement. |
| C099 | VERIFIED-CODE | `ws1_interval_max.py:1-9`; `ws1_freeze_tables.py:6-10,53`; `se:28-31`; `r1_tables.json` MAX knots = interval maxima rounded up (k=25 carries the (25,29] max 99.93 -> 100) | Grid-point maxima printed as diagnostic only [`ws1_freeze_tables.py:75-77`]. |
| C100 | VERIFIED-CODE | `se:35` `(6.0, 4.0)`; `r1_tables.json` p995_raw[6.0] = 3.684, n_real 3,605 (08-13..27) | Rounded up to $0.5. |
| C101 | VERIFIED-CODE | `se:36` `(25.0, 28.5)`; `r1_tables.json` p995_raw[25.0] = 28.441, n_real 3,627; LODO folds 27.5-29.9 | |
| C102 | VERIFIED-DATA | `r1_regime.json` exceedance["25.0"]: over 401 / n 3,627 = 11.06% (grid samples 08-13..27, design 0.5%) | Other knots 3.5-11.7%. |
| C103 | VERIFIED-DATA | `r1_regime.json` p995_early 9.74 (n 1,244, 08-14..18 UTC) vs p995_late 34.45 (n 2,383); `r1_tables.json` freeze_span_reproduction 16/16 on ep < 08-18 | "Week" is loose: the freeze span was 08-14..17 (970 windows per `se:22`; 993 with ep < 08-18 00:00Z), i.e. 4 days. Calm: yes. |
| C104 | VERIFIED-CODE | `se:38,44` last knot (58.0, ...); `settings:100` `twap_zone_s: 58.0`; `se:53-60` clamps beyond | |
| C105 | VERIFIED-CODE | `se:31-33` comment: "re-fit on >=14 real-final days is re-measurement, not relaxing" | Policy restated in code comment; nothing enforces. |
| C106 | VERIFIED-CODE | `se:31-32` comment: "Tuning these to make a window fire is relaxing a bar" | Policy; comment-only. |
| C107 | VERIFIED-CODE | `settings:72` `taker_enabled: false`, `:139` `maker_bid_enabled: true`; `main:2652` MakerBidManager built iff enabled; `main:1084` stamps `signal_leg="deep_proj"` | Data: paper DB 38/38 positions deep_proj (08-18..27); live 60s-era 5/5 deep_proj (02_data_facts). The only leg deploying capital. |
| C108 | VERIFIED-CODE | `settings:161-166` rungs 0.80/0.65/0.50/0.35/0.20; `mb:114-118` identical seed default | `memory/state/maker_ladder.json` could override prices [`mb:119-134`]; file absent in `polybot/memory/state/`. |
| C109 | VERIFIED-CODE | `settings:162-166` frac 0.20 per rung; `mb:150` `usd = round(budget_usd * frac, 2)` | Budget = bankroll x frac x breaker mult [`main:1076-1078`]. |
| C110 | VERIFIED-CODE | `settings:179` `maker_bankroll_frac: 0.15`; `main:1077` default 0.15 | |
| C111 | VERIFIED-CODE | `main:1056-1057` `_mside = "Up" if _mdisp >= 0 else "Down"` from the bridged projection; `main:1080-1081` token by side | |
| C112 | VERIFIED-CODE | `main:1050-1051` `projected_final_twap(..., bridged=True)`; `main:1062,1072` `abs(_mdisp) >= min_need() * twap_margin(TWAP_MARGIN_P995, k)` | `headroom_mult = abs(disp) / P995(k)` passed per rung [`main:1082`, `mb:147-149`]. |
| C113 | VERIFIED-CODE | `settings:162-166` third column 1.0 on every rung; `mb:222-228` `min_need()` = min over rungs | |
| C114 | VERIFIED-CODE | git 856d2c52 2026-08-18 "WS1: interim ladder floor need 1.0 — the in-sample 0.5 grid could not be validated out-of-fit"; `ws1_oos.py:11-16` pre-registered bar + "terminal state (c): stage need 1.0 interim" | The 08-18 run's own output is not in the data dir; the 08-27 re-run (`r1_oos.log`) reproduces the verdict. |
| C115 | VERIFIED-DATA | r1 floor section / `r1_oos.log` (ws1_oos LODO, 08-14..27, k_place [6,25]): need 0.5 fails clause (i) 0.80 rung 16/19 = 84.2% vs 90% and clause (iv) 2 losses on 0.5-only arms (-$18.00, -$4.50); N = 19 OOS fills | RESEARCH.md's original 08-18 figures (17 OOS fills, 6/7) have no artifact; same verdict both runs. |
| C116 | VERIFIED-DATA | r1 (2026-08-27) "Floor: need 1.0 STANDS; 0.5 FAILS the #1 bar" on 15 ET days / 3,695 windows | The re-decision has already run; the CLAUDE.md sentence still reads future-tense ("re-decides"). |
| C117 | VERIFIED-CODE | `settings:167` `maker_k_place_max: 25.0`, `:174` `maker_k_place_min: 6.0`; `main:1054-1055` | |
| C118 | UNVERIFIED | `ws4_k25.py:1-16` implements sweep kinematics + flip race + [6,58]-vs-[6,25] counterfactual; no output artifact anywhere under `scripts/research/data/` (grep "sweep kinematics", "338 clusters", "10 flips" hits REFUTATIONS.md only) | Measured on 60s-era tape 08-14..18 per REFUTATIONS.md:54-55 (so not EXPIRED). r23 (08-27) re-tested k_max in {15,20,25} only — k>25 was never re-measured on the re-fit tables. |
| C119 | UNVERIFIED | Numbers (median 0.00s to the 0.80 rung, 1.79s to 0.20, 338 clusters) exist only as prose REFUTATIONS.md:56-59; `ws4_k25.py:44-60` is the code that would produce them | No log/JSON. |
| C120 | UNVERIFIED | Prose only REFUTATIONS.md:58-60 (0.80: 90% vs 80%; 0.20: 50% vs 20%); flip race N = 10 flips | Sample size 10; no artifact. |
| C121 | VERIFIED-CODE | `mb:269-282`: `signed < self.min_need() * twap_margin(TWAP_MARGIN_P995, k)` -> "projection flipped" / "sign inside noise" -> `_retire`; same `min_need()` gates placement at `main:1072` | Tests `tmb:179,190,199`. |
| C122 | VERIFIED-CODE | `settings:185` `post_close_hold_s: 60.0`; `mb:257` `now - close > post_close_hold_s` -> "post-close hold over" | Test `tmb:277`. |
| C123 | VERIFIED-CODE | `mb:259-267` post-close branch calls `certain_winner()` every tick; winner != side -> "lock missed the winner" | Tests `tmb:238,248`. |
| C124 | VERIFIED-CODE | `mb:230-242` returns None unless `boundary_captured AND strike_reliable` for BOTH boundaries; `mb:264-265` None past `PC_VERIFY_GRACE_S = 5.0` -> "outcome unverified" | Tests `tmb:259,268`. Data: `polybot.log` 08-27 11:40:05 "MAKER OFF Down ... (outcome unverified)". |
| C125 | VERIFIED-CODE | `cl:187-217` `spot_bridge_delta`: anchor = last Binance tick at/before `_last_report_obs_ts` (payload clock), delta = newest - anchor; `cl:258` `spot = _price + delta` when `bridged` | Constants `cl:48-51` BRIDGE_ANCHOR_MAX_AGE_S 2.0, BRIDGE_MAX_DELTA_FRAC 0.01. |
| C126 | VERIFIED-CODE | `cl:194-217`: cold ring / no newer tick / no anchor / anchor older than 2.0s / abs(delta) > 1% of anchor each return 0.0 = plain projection | A None projection (stale spot, coverage hole) is None for both plain and bridged — the "collapse" is about the delta term only. |
| C127 | VERIFIED-CODE | `mb:333-352` vwap = sum(filled x price)/sum(filled) over all rungs -> one `book_maker_fill`; `base:379-433` one positions row | Test `tmb:411` booked notional = sum of rungs. |
| C128 | UNVERIFIED | Inputs are comment-only: strictly-below rule from "102 live placements, zero fills" (`mb:20-23`, 08-13 post-close 0.99 probe, pre-era); 135-sh queue from a book watch "49 windows / 30k levels, 08-17" (`mb:46-49`, no artifact; `ws3_queue.py:7` calls it "a one-day book-watch"). Only live cross-check: `h3_live_fill_check.json` / h3_report.md:61-90 — the 5 era live fills (08-14/15) sat in windows with 108-582 own-token prints <= fill price (consistent, N=5) | "Conservative" is disputed in-repo: h3 leaves a <=15-17% invisible-fill upper bound open; RESEARCH.md 2b calls the GTC-clock distortion "two-sided ... direction unknown, not conservative". |
| C129 | VERIFIED-CODE | `mb:210-211` `px < r["price"] - 1e-9` -> `r["filled"] = r["shares"]` | Test `tmb:128-140`. |
| C130 | VERIFIED-CODE | `mb:212-218` at-price: accumulate `at_px_vol`, `credit = min(shares, max(0, seen - AT_PRICE_QUEUE_SH))` | Test `tmb:311-328`. Docstring drift: `mb:20-23` says at-price prints "never count"; the executing code credits beyond 135 sh. |
| C131 | VERIFIED-CODE | `mb:46` `AT_PRICE_QUEUE_SH = 135.0` | Provenance (49 windows / 30k levels, 08-17) is comment-only; the nightly `queue_depth_read` watches it (needs >= 50 sweeps) [`alw:254-330`; `main:528-576`]. |
| C132 | VERIFIED-CODE | `mb:51-53` comment "snapshot queue models are BANNED"; `on_print` uses the constant, never a book snapshot | Policy; enforced only in the sense that no snapshot code exists. |
| C133 | VERIFIED-CODE | `pt:63-80` both `place_gtc_bid` and `cancel_gtc` await `_simulate_gtc_latency()` | Test `tmb:166`. |
| C134 | VERIFIED-DATA | `docs/audit/data/latency_stats.json` keys = n/p50_ms/p99_ms/max_ms/sign/post/last_updated (2026-08-13), no `gtc` section; 08-27 ping "GTC RTT unmeasured — only 0 GTC samples" [`polybot.log:805-817`]; `lt:650,672` record GTC RTT but no live GTC since 08-15 | Nuance: paper's 56ms table itself came from `smoke_gtc_test.py --samples 12` on the box (`pt:293-294`); what is unmeasured is the live ladder's in-anger place/cancel RTT. |
| C135 | VERIFIED-CODE | `pt:297-299` `_GTC_LATENCY_QUANTILES` (0.50, 0.056); `ws2_ladder_replay.py:42-43` PLACE_LAT 0.056 / CANCEL_LAT 0.054 | N = 12 smoke samples per the comment; commit 688bb2e8 2026-08-12; raw samples not in the repo. |
| C136 | UNVERIFIED | Only source is RESEARCH.md:86-89 prose ("Reconstructing live fill 334 from tape needs ~500ms ... $11.21 vs $3.74 actual"); no script or artifact; latency_report.md:20 and r23_report.md:109 both label it "reconstructed ... unmeasured" | The anchor is real: live DB id 334 size $3.7356 (08-14 21:44Z). The 500ms figure is not reproducible from the repo. |
| C137 | UNVERIFIED | Depends on C136; RESEARCH.md:88 "roughly twice as fast" is the only source | Arithmetic note: 56ms vs ~500ms is ~9x, not 2x; RESEARCH.md 2b says the distortion's direction on c/sh is unknown. |
| C138 | VERIFIED-CODE | `base:411-415` `fee_in_shares = 0.0` in `book_maker_fill` (since e996a414 2026-08-17); h1_report.md:172-174 `fee_bps=0` on 3,321,809 tape prints 08-14..21 | Tape zero-fee covers takers too, so it cannot isolate makers. The live DB's own 5 era maker fills (08-14/15, pre-e996a414) carry model fee deductions (fees 0.95/0.09/0.25/0.08/0.08, `maker_fill=0`) — a booking artifact, not chain evidence. |
| C139 | UNVERIFIED | grep "274/274", "274 " across scripts/, docs/, data/, polybot/ -> no artifact; `base:411-413` cites a different check (1,751 live fills, 08-10, pre-era) | The 274 rows cannot be the bot's own ledger (5 post-rule live fills exist). |
| C140 | UNVERIFIED | Not code-enforced: `alw:183-194` returns raw metrics, no threshold; 01c §4.6. Text appears in `settings:5` comment and `scripts/sniper_shadow_status.py:12` (both ">=6 clean days") | Policy; the two code-side texts agree on the day count. |
| C141 | UNVERIFIED | Not enforced. `settings:5` comment ">=20 filled windows" vs `sniper_shadow_status.py:12,93` ">=40 fills" | Discrepancy between the two code-side statements of the bar; neither is checked by code. |
| C142 | UNVERIFIED | Not enforced. `settings:6` "+5c/sh" vs `sniper_shadow_status.py:13,94` "+0.02" vs the nightly Shut-off line "+2.0c" [`main:2838-2846`] | Three code-side thresholds disagree; none enforced. |
| C143 | UNVERIFIED | `usd_per_day` is computed and returned [`alw:143,189`] but never compared to 0; no code text states it as a bar | Only RESEARCH.md:48-49 ("dollars > 0"). |
| C144 | VERIFIED-CODE | `main:2773-2782` paper mode -> `live_health_read(PAPER_DB, validation_epoch)`; `alw:102-104` `exit_timestamp >= since_iso`; `settings:79` epoch 2026-08-27T19:28Z | Data: fills by epoch in 02_data_facts; current epoch has 0 fills (snapshot 08-27 20:27Z). |
| C145 | VERIFIED-CODE | `settings:72` `taker_enabled: false`; git 7fd133db 2026-08-18 "WS3: lock-dip taker staged DORMANT"; `main:1033-1041` | |
| C146 | UNVERIFIED | Rests on ws3_dips.py output, which is absent (and its input `winner_books.jsonl.gz` is not in `scripts/research/data/`) | Data hint only: 46 "taker dormant: would have fired" lines 08-19 17:29 -> 08-25 17:29 (`polybot.log.1`) on the since-superseded thin tables — neither confirms nor refutes. |
| C147 | UNVERIFIED | No artifact; the numbers exist only in `settings:73-74` comment, `main:1028-1029` comment ("1 reachable dip per 5 days measured 08-14..18"), RESEARCH.md:104,391 | `ws3_dips.py` is the tool; cannot be re-run locally (missing winner_books). |
| C148 | UNVERIFIED | Same as C147 (`ws3_dips.py:167` prints "FOK-reachable at RTT 0.436s") | |
| C149 | VERIFIED-CODE | `ws3_dips.py:11-12` pre-registered bar "dips >= 1 per 3 trading days AND harness EW >= +2c/sh after the real taker fee"; `settings:74` | Bar text lives in the research script's docstring. |
| C150 | UNVERIFIED | Interpretive claim; no measurement artifact | |
| C151 | VERIFIED-CODE | `se:84-149` `evaluate_twap_lock` intact; `main:1018-1026` still called every tick in the zone | |
| C152 | VERIFIED-CODE | `main:1033-1041` `_emit_gate_skip(cid, "taker_dormant", "taker dormant: would have fired ...")` | Data: 46 such lines 08-19..08-25 in `polybot.log.1`, 0 in `polybot.log` (08-26/27). |
| C153 | VERIFIED-CODE | RESEARCH.md:108-111 "Re-arm condition: re-run ws3_dips.py ... re-arm only if reachable dips >= 1/3 days AND the harness clears +2c/sh" | Pointer claim: the text exists. |
| C154 | VERIFIED-CODE | `se:89` default `require_max_tier=True`, `se:121` `need = mmax if require_max_tier`; `settings:99` true; `main:1026` | Tests `tls:63,81,91`. |
| C155 | VERIFIED-DATA | `r1_regime.json` exceedance: the frozen p99.5 knots were exceeded 3.5-11.7% per knot on 3,596-3,678 samples/knot, 08-13..27 (design 0.5%) | `se:102-106` cites three 30s-era breaches (pre-era origin); the 60s-era data independently supports the statement. |
| C156 | UNVERIFIED | Appears only in `settings:96-97` comment and RESEARCH.md:106 ("one -95.3c breach vs thirteen +5c wins"); no analyze_twap_lock output artifact in the data dir | |
| C157 | VERIFIED-CODE | `settings:103` `twap_k_min_s: 6.0`; `se:112` `k < k_min_s -> SKIP`; `main:1023` | Test `tls:190`. |
| C158 | VERIFIED-CODE | `main:1017` `projected_final_twap(_w_ts + 300)` (default `bridged=False`); `cl:228-231` "The taker NEVER uses it" | |
| C159 | VERIFIED-CODE | `se:132-133` `edge = prob - ask; if edge < sniper_min_edge -> SKIP` (0.04 at `settings:108`) | Equivalent to ask <= tier_prob - sniper_min_edge. |
| C160 | VERIFIED-CODE | `settings:113` `sniper_fok_slip: 0.01`; `main:1205-1212` `price = snap_to_tick(max(price, min(price + slip, prob - min_edge)))` | "One tick" holds for the 0.01 in-window tick size. |
| C161 | VERIFIED-CODE | `se:138-141` `kelly = self._kelly(ask + sniper_min_edge, ask, fee_rate)` — prob anchored to the ask, never to the tier | Test `tls:209`. |
| C162 | VERIFIED-CODE | `main:1003-1013` (trading_enabled, zone, strike-trusted), `main:1145-1193` (breaker, correlation, deployed cap, depth cap, net-edge, $1 floor); full stack in 01b §3 [`main:895-1392`] | The entire taker path is dormant behind `taker_enabled: false` [`main:1033-1041`]. |
| C163 | VERIFIED-CODE | `lt:81` `_FILL_AUDIT_DELAY_S = 8.0`; `lt:881` sleep before the entry audit; `base:371-373` `_schedule_fill_audit` | |
| C164 | VERIFIED-CODE | `base:547-551` `fees = entry_fee_usd + exit_fee_usd_val`; `base:213-216` entry_fee_usd = (size/entry_price - shares_held) x entry_price | Nuance: the column holds the USD value of the MODELLED share deduction (+ modelled exit fee), never a value read from the exchange; "share-denominated" is loose — the unit is USD. |
| C165 | UNVERIFIED | Mechanism claim about Polymarket; the bot models it (`base:138-140,160-168`: fee = 0.07 x sh x p x (1-p), collected in shares on BUY); h1_report.md:172-174 asserts fees are "charged off-tape via USDC debits" but that is itself unartifacted | No USDC-debit reconciliation artifact in the repo. |
| C166 | UNVERIFIED | grep "326 rows", "326/", "fee/model", "median 1.000" -> no artifact; the live DB has only 5 post-rule fills (all maker) | The 326 rows cannot be the bot's own ledger. |
| C167 | CONTRADICTED | Sell code exists: `BaseTrader.close_trade` [`base:442-514`] -> `_execute_sell` [`base:475`]; `PaperTrader._execute_sell` [`pt:86-105`]; `LiveTrader._execute_sell` [`lt:632-637`]; `warm_sell_signature` [`lt:1091`, `pt:107`]; `settings:38` `fok_spread_cross_floor` "scalp-exit"; live DB has 19 `exit_reason=scalp` rows (pre-07-08) | What IS true: zero call sites outside `polybot/tests/` (grep `.close_trade(` / `_execute_sell(` / `warm_sell_signature(`), i.e. no sell path is WIRED. "No sell path exists in the codebase" is literally false. |
| C168 | VERIFIED-CODE | `ws2_ladder_replay.py:14` "P&L: hold to resolution, maker pays no fee" (the engine behind r1/r23); `ws3_dips.py:11-12` taker harness EW after the real taker fee | Both measurement harnesses hold to resolution; the realized ledger has only `resolution` exits in the 60s era (02_data_facts). |
| C169 | VERIFIED-CODE | `alw:64,79-84` docstring; `alw:172-177` sets `tripped`; `alw:194` returned as key `kill_rule_tripped` | It is a dict key of `live_health_read`, not a function. |
| C170 | VERIFIED-CODE | `main:2738-2739` the nightly job runs whenever `trading_enabled` is true; `main:2773-2782` reads the live ledger in live mode | Nothing separately "arms" it; it is on in both modes. |
| C171 | VERIFIED-CODE | `alw:159` `breach_losses = count(lock_dip fills with w == 0)`; `alw:172-173` any breach -> `tripped = True` | Test `tlh:158`; `tlh:169` maker losses do not trip the breach clause. |
| C172 | VERIFIED-CODE | `se:89,121` max tier only when `require_max_tier`; `settings:99` true | Caveat: holds only while `require_max_tier` stays true — the kill rule itself does not check the tier. |
| C173 | VERIFIED-CODE | `alw:166-171` per leg: `mean(daily $ over trailing4_days, zero-filled) < 0.0` -> tripped; `alw:172-177` | Test `tlh:138,283`. |
| C174 | VERIFIED-CODE | `alw:150` `n_cal_days` = calendar span first->last fill day; `alw:166-169` `if n_cal_days < 4 or leg_fills < 5: continue` | Tests `tlh:102,147,262,274`. "4 ET days" is calendar span, not 4 days with fills. |
| C175 | VERIFIED-CODE | `alw:164-169` thin legs skipped (no trip); `alw:174-175` `tripped = None` ("still accruing") | Test `tlh:147` sparse_fills_keep_accruing. |
| C176 | VERIFIED-CODE | `alw:161-164` comment; `tlh:298` `test_quiet_days_then_one_loss_still_does_not_trip` encodes the scenario | The "measured 08-18" event (08-18 13:45 flip, -$4.50) exists only as RESEARCH.md:27-30 prose; the rule is code- and test-enforced. |
| C177 | VERIFIED-CODE | `main:1003` one `if lw_cfg["trading_enabled"]` encloses both the taker eval and the ladder placement (`main:1003-1097`); `main:2212-2213` wake gating; `settings:68-71` | |
| C178 | VERIFIED-CODE | `rec:325-357` served vs trusted capture, `abs(served - cap) > 0.005` -> `on_source_mismatch`; `main:2679-2698` sets `config["late_window"]["trading_enabled"] = False`, CRITICAL log, Discord page | Test `polybot/tests/test_recording.py:421`. Data: 0 fires, 32 "SOURCE CHECK SKIPPED" 07-13..08-27 (02_data_facts). |
| C179 | VERIFIED-CODE | `settings:63-65` "the BINDING gate is the paper-shadow's realized fills. DO NOT enable on faith"; `alw:5-7`; `main:2825` the SIM never feeds the verdict | Policy; restated in config/code comments, not enforceable. |
| C180 | VERIFIED-CODE | `main:2736-2737` "kill-rule verdict is driven by the realized ledger"; `main:2825` `kt = live["kill_rule_tripped"]`; `alw:5-7` | Policy with a code footprint (verdict source). |
| C181 | VERIFIED-CODE | `open_trade` called only at `main:1310` (taker path); `book_maker_fill` only from `mb:349`; no other entry booking (01b §6.4: the boot reconcile path books exits only) | |
| C182 | VERIFIED-CODE | `main:1145-1147` `size = round(bankroll * signal.kelly_size * kelly_mult, 2)`, `kelly_mult = breaker.kelly_multiplier` | Dormant path (taker). The ladder budget also carries the breaker mult [`main:1076-1078`]. |
| C183 | VERIFIED-CODE | `main:1149-1154`; `polybot/execution/correlation.py:36-58` rho +0.75 same side / -0.25 opposite -> 0.35 / 0.55 / 0.70 / 0.90 | Tests test_correlation.py:8-29. The rho constants are asserted, not measured. |
| C184 | VERIFIED-CODE | `main:1156-1159`; `max_bankroll_pct` from `config["execution"]["max_bankroll_deployed"]` [`main:2041`]; also enforced at booking [`base:401-402`] | |
| C185 | VERIFIED-CODE | `settings:21` `max_bankroll_deployed: 0.8` | |
| C186 | VERIFIED-CODE | `main:1163-1172` `max_fill = side_depth * max_fill_pct; if size > max_fill: size = max_fill` | Preceded by the `min_book_depth_usd` (50) thin-book veto. |
| C187 | VERIFIED-CODE | `settings:23` `max_book_fill_pct: 0.5`; `main:1164` default 0.50 | |
| C188 | VERIFIED-CODE | `main:1186-1193` `if size < 1.0: _record_skip("min_size")` + ghost; ladder floor `mb:43` `MIN_NOTIONAL_USD = 1.0` | |
| C189 | VERIFIED-CODE | `se:138-141` prob = ask + sniper_min_edge (market-anchored); `se:151-160` `net_b = b * (1 - fee_rate)` | Fee enters as a flat 7% haircut on the odds — a third fee form beside the `rate*sh*p*(1-p)` curve [`base:160-162`] and the 0.0175 flat proxy [`base:144`]. |
| C190 | VERIFIED-CODE | `se:160` `return max(0, raw * self.kelly_fraction)` | |
| C191 | VERIFIED-CODE | `settings:12` `kelly_fraction: 0.08`; wired `main:2457-2459`; `se:80` default 0.08 | |
| C192 | VERIFIED-CODE | `cb:17-18` `_TIERS = [100, 150, 200, 300, 400, 600, 800, 1000, 1500, ...]`; `cb:21-29` `_locked_tier` | Tests `tcb:27,167,207`. |
| C193 | VERIFIED-CODE | `settings:15` `floor_pct: 0.85`; `cb:60,110` `floor = round(tier * floor_pct, 2)`; `main:2513` | |
| C194 | VERIFIED-CODE | `cb:91-98` `min_multiplier + (1 - min_multiplier) * sqrt(ratio)`; `settings:16` `min_multiplier: 0.4`; `main:2514` | Tests `tcb:199,237`. |
| C195 | VERIFIED-CODE | `cb:107-110` tier/floor change only `if new_tier > self.locked_tier` | Test `tcb:175` floor_never_goes_down. |
| C196 | VERIFIED-CODE | `main:1806,1944` `db.set_peak_bankroll(breaker.peak_bankroll)`; `main:2522-2527` `restore_from_peak(persisted_peak, init_bankroll)`; `cb:115-124`; `polybot/db/models.py:69,491-503` `peak_bankroll` table | Data: paper DB peak_bankroll $400.00 (02_data_facts). |
| C197 | VERIFIED-CODE | `main:1073-1078` `_mbudget = bankroll * maker_bankroll_frac * kelly_multiplier` with comment "budget is a flat bankroll fraction, not Kelly"; `settings:176-179` | The breaker multiplier still scales it. |

## Summary

Rows C089–C197: 109 claims, each exactly once.

| disposition | count |
|---|---|
| VERIFIED-CODE | 81 |
| VERIFIED-DATA | 9 |
| UNVERIFIED | 18 |
| CONTRADICTED | 1 |
| EXPIRED | 0 |

**CONTRADICTED**
- C167 — "No sell path exists in the codebase": `close_trade` / `_execute_sell` / `warm_sell_signature` exist in base, paper and live traders (and a `scalp-exit` config knob); they have zero callers outside tests. The true statement is "no sell path is wired".

**EXPIRED** — none. This range is 08-18+ material; the only pre-era origins found (the 08-13 "102 placements / 0 fills" basis of the strictly-below rule in C128, the 08-12 GTC smoke samples behind C135, the 30s-era breaches cited in the `se:102-106` docstring for C155) concern queue mechanics, GTC RTT, or are independently re-supported by 60s-era data, so they were not marked EXPIRED.

**UNVERIFIED — grouped by cause**
- k>25 refutation (C118, C119, C120): `ws4_k25.py` exists, no output artifact; measured 08-18 on 60s tape; never re-run on the re-fit tables (r23 tested only k_max 15/20/25).
- Taker dormancy numbers (C146, C147, C148, C150, C156): `ws3_dips.py` exists, no output and its input `winner_books.jsonl.gz` is absent locally; the 60s-sim "one p99.5 loss" has no analyze_twap_lock artifact.
- Fee re-verifications (C139 274/274, C165, C166 326 rows): no artifacts anywhere; the bot's own live ledger has 5 post-rule fills, all maker, all booked pre-zero-fee-code with model deductions.
- GTC clock (C136 ~500ms reconstruction, C137 "twice as fast"): prose-only; the stated ratio does not follow from 56 vs 500 (~9x).
- Paper bar text (C140–C143): not code-enforced anywhere; the three code-side statements disagree (settings comment >=20 windows / +5c; `sniper_shadow_status.py` >=40 fills / +2c / t_day>=2 / p10>0; nightly Shut-off line +2.0c).
- Paper fill rule "live-calibrated and conservative" (C128): calibration inputs comment-only; one live check N=5 is consistent; "conservative" is disputed by h3 (<=15-17% invisible-fill bound) and RESEARCH.md 2b (direction unknown).

**Notable nuances on VERIFIED rows (for the parent's doc-drift list)**
- C116: the ">=14-day re-fit re-decides `need`" already happened 08-27 (need 1.0 stands); CLAUDE.md still reads future-tense.
- C103: the 08-18 freeze span was 4 days (08-14..17), not "one calm week".
- C130: `mb:20-23` docstring says at-price prints "never count"; code credits beyond 135 sh.
- C134/C135: paper's 56ms table IS a measurement (12 smoke samples, 08-12); what is unmeasured is the live ladder's in-anger RTT — `latency_stats.json` has no `gtc` section.
- C138: the five 08-14/15 live maker fills in the DB carry model fee deductions (booked before e996a414 2026-08-17 made maker booking fee-free) — the ledger is not evidence of fee-free makers either way.
- C164: the `fees` column is USD (modelled share deduction x entry price + modelled exit fee), not literally share-denominated.
- C172: "every lock_dip fire is max-tier" is a config invariant (`require_max_tier: true`), not checked by the kill rule.
- C174: ">=4 ET days" is the calendar span from first to last fill day, zero-filled, not four days with fills.
- C182–C188: the whole taker sizing chain is dormant (`taker_enabled: false`); only the breaker multiplier reaches deployed capital today, via the ladder budget.
- C189: Kelly's fee form is `b x (1 - 0.07)` — a third fee convention beside `rate*sh*p*(1-p)` and the 0.0175 flat proxy.
